# SESSION_STATE — MTG DNA

## Cold Start Prompt
Priority: **⚠️ BEN MUST RUN MIGRATION `017_drop_maybeboard.sql` IN THE SUPABASE SQL EDITOR — it DELETES every deck_cards row in the 'maybe' section (IRREVERSIBLE; deck_card_tags cascade with them). His call was delete, not migrate. The app already ignores 'maybe' rows, so they're inert until he runs it — the only cost of waiting is dead rows. The file has a sanity-check SELECT to run first.** 2026-07-14 (DEVICE-PASS UAT ROUND 3 — Ben dogfooded a full deck build end-to-end) SHIPPED — 7 items, all on `main`, whole-project lint clean + prod build each, verified lint+build ONLY (DEVICE RE-PASS OWED). **The headline finding: he built most of the deck in magikdex but GOT STUCK ON LANDS and had to leave for EDHREC — so the mana-base story was this round's priority.** Commits: (targets) `fd8152a` WREC band cells read count/target from CATEGORY_META (5/10, 37/30) — plain, NO on/over/under tint (Ben: "adds too much to look at, there's already a lot with that window open"). (wrec calculus) `b5418b0` the maybeboard was polluting BOTH WREC readouts — LegendIdentity bars now filter tagRows to section 'decklist' and ReviewScreen's band counts only 'decklist:' keys. (search) `3b5d5e3` the deck-list search draft moved from ReviewScreen local state into Brew session state — backing out (or a search dealing a stack) unmounted the screen and ate the typed Scryfall syntax, forcing a retype; cleared on resetBrew. (mdfc) `2f3d39f` type bucketing now uses the FRONT face (`type_line.split("//")[0]`), so MDFCs like Malakir Rebirth ("Instant // Land") move OUT of LANDS into Instants/Creatures — Moxfield's model — and the LANDS header carries `· N including mdfc` (isMdfcLand: back face is a Land, front isn't). (lands door) `b7cc540` **the fix for the EDHREC detour** — a `+ lands` action on the LANDS group header runs `t:land -t:basic` through runGlobalSearch (colour identity + legality + deck-exclusion + EDHREC order come free), dealing the shocks/fetches/bonds he wanted but couldn't name. Basics excluded on purpose → (basics) `3915fda` basics and "any number" cards get a `− N +` quantity stepper on their row (isStackable mirrors SwipeScreen's); the stepper stops its own pointerdown so tapping it can't also open the row's carousel. (maybeboard) `d68032f` **THE MAYBEBOARD IS GONE** — it polluted the counts AND its ↓ gesture kept firing when Ben meant "close the carousel": ↓ now CLOSES the carousel (uniform model: ← → browse, ↑ decisive action = mainboard in brew / cut in review, ↓ exit); BACK+DONE removed from the swipe (pull-down replaced them → brew swiping reserves almost no bottom space, card gets the screen); removed maybeboard state/props/sections + handleMoveCard + lib `moveDeckCard` (nowhere to move to) + the inline row grid (its last consumer was maybe rows — this also resolves the round-2 asymmetry flag); imports DROP a provider's sideboard; migration 017 deletes the rows. **NOTE: simplifying Brew let the React Compiler lint analyze it for the first time, surfacing 4 PRE-EXISTING patterns (react-hooks/immutability hoisted-call, ×2 react-hooks/refs latest-ref + ref-read-in-render, react-hooks/set-state-in-effect async load) — documented + scoped-disabled rather than refactored blind; they predate this batch and are worth a real look someday.** (anchor) `97f987a` closing the carousel scrolls the deck list to the card you were viewing (once per anchor, after its cardData resolves so grouping has settled) instead of rubber-banding to the top. STILL OPEN: **migration 017 (above)**; Ben's device re-pass (esp. the lands door + basics stepper — did they end the EDHREC detour? — the ↓-to-close gesture, and the anchor-on-close); the standing follow-ups — (A) share-ready pass for a Bluesky contact (Vault FORM RECOVERY + missing VERCEL ENV VARS), (B) preprod/staging workflow; backlog: Moxfield UA email→Vercel env, WREC chip-color steel decision, SearchScreen/narrow-engine teardown (PileScreen is also dead code carrying maybe refs), soft-corner/chip-form design session.** Prior: **2026-07-14 (DEVICE-PASS UAT ROUND 2) SHIPPED — a second device pass; 4 items, all on `main`, whole-project lint clean + prod build each, verified lint+build ONLY (DEVICE RE-PASS OWED). This round consolidates WREC into ONE tagging surface (the review carousel) + a read-only list. Commits: (copy) `5ad174c` WREC explainer reworded — "named in honor of the great Rachel Weeks" + "treat the targets as guidelines, not rules" framing (Ben's Barbossa note). (band key) `b86947f` the WrecBand composition strip now shows each role's ICON above the count+label, so it doubles as the legend/key (zero cells dim the icon). (carousel tagging) `d14a637` hand-mode SwipeScreen shows the 5 WREC roles as icon tap targets BELOW the card (wired cardTags + handleToggleTag from Brew; applied = filled category color, solid=user/dashed=auto, unapplied dim; card reserves +56px bottom room). **Brew-swipe deliberately excluded — those cards aren't in the deck yet, so no deck_card row to tag.** (grid retired) `e4b7bda` a DECKLIST row tap now opens the review carousel AT that card (via onHand→enterHandMode startName) instead of the inline mini-card + WREC grid; tagging lives in the carousel now. **MAYBEBOARD rows KEEP the inline expander** (static card preview + tag targets + move-to-mainboard) because maybe cards aren't in the flip carousel — it's their only tag/promote path; the mini-card there is now a static <div> (no carousel jump). Row badges + the band key remain the at-a-glance "what's tagged." **ASYMMETRY FLAGGED FOR BEN: decklist rows → carousel, maybe rows → inline grid. If he wants maybe unified too, options are (a) a swipe-right-to-promote gesture on maybe rows + kill their grid, or (b) include maybe cards in a carousel mode — his call.** STILL OPEN: device re-pass on all of the above (esp. carousel tagging feel + the decklist-tap-to-carousel, and whether the maybe asymmetry bugs him); the WREC info copy is his to refine further; plus the standing follow-ups — (A) share-ready pass for a Bluesky contact (Vault FORM RECOVERY + missing VERCEL ENV VARS), (B) preprod/staging workflow; and backlog: Moxfield UA email→Vercel env, WREC chip-color steel decision, SearchScreen/narrow-engine teardown, soft-corner/chip-form design session.** Prior: **2026-07-14 (BEN'S DEVICE-PASS UAT) SHIPPED — Ben ran a live device pass on magikdex.vercel.app and shared 6 observations bit-by-bit; all 6 fixed on `main`, whole-project lint clean + prod build each, still verified lint+build ONLY in sandbox (DEVICE RE-PASS OWED). Commits: (deck count) `ea4836b` deckTotal counted every deck_cards row (mainboard + maybeboard) + commander → Box tile read e.g. 74/100 with 2 maybeboard cards; now excludes 'maybe'/'pile' sections so the count is ALWAYS just the main board + commander (added `section` to the 3 queries feeding deckTotal; rows w/o section still count, backward-compatible). The ReviewScreen header subline (batch-3 item 4) already excluded maybe, so header + box now agree. (search) `225c696` a removed card stayed unsearchable ("every card matching is already in your deck") — buildSwipeCards unions a start-of-session row snapshot (existingCardRows) with the live decided-names set; the live set self-updated on removal but the snapshot didn't → handleRemoveCard now prunes the snapshot once a card's last copy leaves the deck. (review-at-card) `a913397` enterHandMode takes a startName and seeds handIndex to it; tapping a row's mini-card now opens the SWIPEABLE review carousel AT that card (the static full-size overlay from batch-2 item 12 is REMOVED — the zoom IS the stack, matching the swipe instinct), and hitting 'review' with a card expanded starts the flip pass on that card (else still resumes at last-viewed). (deeper-black) `2fdff68` idle decklist rows painted an opaque var(--bg) on their own transform-promoted GPU layer, compositing a hair darker than the page → the opaque bg now applies ONLY while a row is actively being swiped (covers the delete zone); idle rows are transparent. (WREC info) `9bf9764` the (?) beside the WREC header now opens an IN-APP explainer (what WREC = Wachel Reeks Effectiveness Coefficient is + the 6 roles with target counts) with the Command Zone video link at the BOTTOM, instead of jumping straight to YouTube. **STILL OPEN: Ben's device re-pass on all of the above (esp. the review-at-card carousel, the deck count now reading mainboard-only, the WREC info box copy — he may want to refine wording); the TWO non-ticket follow-ups he flagged earlier — (A) share-ready pass for a Bluesky contact (Vault FORM RECOVERY + missing VERCEL ENV VARS) before sending the link, (B) a preprod/staging workflow so iteration stops hitting live; plus the standing backlog: Moxfield UA email→Vercel env, WREC chip-color steel decision, SearchScreen/narrow-engine teardown, soft-corner doctrine + chip-form design session.** Prior: **2026-07-14 (SONNET-UAT BATCH 3) SHIPPED — Ben's 7-item bug list from a second Sonnet UAT session. All 7 on `main`, whole-project lint clean + prod build each, verified lint+build ONLY (no Supabase session in sandbox → live screens un-drivable; DEVICE PASS OWED). Commits: (item 1) `a0075df` the paste-deck/Add-Legend sheet AUTO-CLOSES after a successful import — it was designed to dismiss itself after the result summary (the parent never closes it) but never did; now flashes "imported N cards" for 1.2s then closes so the new legend shows in the box (timer cleared on reopen/unmount). (item 2) `57071d7` WREC 6-cell TAGGING GRID — two fixes: (a) the optimistic toggle used a stale outer `had` INSIDE the setCardTags updater instead of recomputing from `prev`, so a stale read could append a DUPLICATE tag and leave a deselected cell stuck filled + un-clearable → now computes `has` from `prev` (idempotent); (b) each grid cell now renders the WREC ICON badge (same vocabulary as the collapsed row chips) with an unmistakable applied state — applied fills in its CATEGORY color (solid border = user, dashed = auto-suggested), unapplied is dimmed/empty (opacity 0.5, no fill). Ben's exact ask: icons persist while selected, cell empties when deselected. (item 3) `c9dffce` the split/aftermath/adventure FLIP control is gone — it now gates on a real back-face image (`card_faces[1].image_uris`), so only TRUE double-faced cards get flip; split-type cards (one shared image) are read by rotating the phone (the cheaper of Ben's two options). (item 7) `d450d40` the "ALL CARDS SEEN" end screen kills the dead pile — no "0 pile ·" count, no VIEW PILE; just `{mainboard} · {maybe}` + VIEW DECK / SEARCH MORE (buttons also dropped their leftover border-radius). (item 4) `83e48b2` deck card count restored NEAR the commander header — a `{decklist.length + 1} cards` subline under the commander name (commander + mainboard, matching the Box deck-total convention; was missing in the Zevlor session). (items 5-6) `8b381df` a (?) help icon beside the WREC header opens the Command Zone deck-building guide in a new tab — **used the clean URL `https://youtu.be/OSNV6224cHg`; Ben's given URL had a malformed `?is=` share param (typo for `?si=`) that I dropped since the share token isn't needed to play the video.** **TWO NON-TICKET FOLLOW-UPS Ben flagged (NOT built this session): (A) Web MVP needs to be share-ready for a Bluesky contact — do a pass on the two known criticals (Vault FORM RECOVERY + missing VERCEL ENV VARS) before sending the link; (B) a preprod/staging workflow so iteration stops hitting the live app. Both are their own tasks — surface them next.** STILL OPEN from before: Ben's device pass over batches 2 AND 3 (esp. the steel recolor on OLED, the tagging-grid badge fix, the row mini-card overlay, the search+sort panel, persistence resume); item-1-of-batch-2 Moxfield UA email→Vercel env; the WREC chip-color steel decision (batch 2 flag); SearchScreen/narrow-engine dead-code teardown; soft-corner doctrine + chip-form design session.** Prior: **2026-07-14 (SONNET-UAT BATCH 2) SHIPPED — Ben's 19-item list from a Sonnet UAT session (supersedes/corrects parts of the earlier 12-item batch). Item 1 is BLOCKED not code (Moxfield import 403 — the api/deck.js `MOXFIELD_UA` path is already wired; needs Ben to email Moxfield support for an approved UA, then set MOXFIELD_UA in Vercel env). The other 18 shipped in 11 commits on `main`, whole-project lint clean + prod build each, verified lint+build ONLY (no Supabase session in sandbox → live screens un-drivable; DEVICE PASS OWED). Commits: (item 4) `8205c96` exclude-lands seed default flipped ON→OFF (brewDefaults.js). (items 6+8) `f7ae278` the decklist section header IS the WREC disclosure trigger now (no DECKLIST label/count, just `WREC ▾`; an active filter named beside it); the disclosed band moved out of the sticky commander header to open under its trigger and NO LONGER auto-closes on category select (dashboard, not menu — only the trigger/reload closes it). (item 7) `320bf8d` small 27×38 legend card thumbnail in the review header corner (fed by session legend image_uri). (item 9) `9824c7c` real header hierarchy — section rank (WREC/MAYBEBOARD) = Zilla Slab 700/17px, group rank = mono 13px text2, no more paragraph-sized structural text. (item 10) `a1b3ab3` LegendIdentity WREC bars split the card-frame height EVENLY (flex rows, no clipping) + CARD-ADVANTAGE/MASS-DISRUPTION nowrap-ellipsis (tighter tracking). (item 15) `dd38d9d` decklist bottom-right relabeled done→`review` and it RESUMES the flip pass at the last-viewed card (Brew keeps handIndex, clamped on entry). (items 16+17) `ff4312e` gesture hint centered horizontally (both modes) + "mainboard"→"deck" in the quick-brew hint. (item 2) `301fccc` LIGHT MODE KILLED — tokens.js collapsed to one flat palette, ThemeContext drops mode/toggleTheme/localStorage, every `mode==="light"?…:…` ternary across 12 shell files resolved to its dark branch, Settings theme-toggle row removed. (item 3) `aaa7a22` JACKSON STORM steel recolor — near-black grounds #08090c/#12151a, one electric-steel accent #38bdf8 replacing all gold/amber, dimmer steel #5f9bbf secondary, brighter red #e0555f; amber/green token keys renamed accent/accent2 (tokens.js + BREW bridge) so no name lies; gc-glow + GAME CHANGER plate + tier badges recolored. **DELIBERATELY NOT recolored: WrecBand WREC_CHIP_COLORS (ramp/disruption/plan amber-green-red) — that's a CATEGORICAL data-viz palette encoding category identity, not app chrome; folding to one blue would erase the distinction. Ben's call whether to restyle it steel.** (items 11-13) `78977d1` decklist row TAP now expands to a 104px mini-card (the real card) on the left + WREC tag targets (5 tags + move) beside it; tapping the mini-card blows it up full-size (same overlay grammar as the commander name); the persistent tag icon stays in the row header through expansion (was vanishing); the redundant oracle-text block + oracleOf helper removed (the card image carries the text); action chips dropped their pill border-radius to honor the no-radius rule. **"6 WREC tags" read as the existing 6-CELL grid (5 tags + move), NOT a new taxonomy category — adding a 6th would touch the wrec_tag DB enum + auto-tagging + both readouts (out of scope).** (item 14) `edfd479` Moxfield's TWO-axis organize model replaces the single view key: GROUP BY (type/mana value/color/none) × SORT BY (name/mana value), independent, persisted per deck, migrated from both older stored shapes. **EDHREC-rank sort omitted BY DESIGN — CARD_CACHE_COLS (what the deck rows read) has no edhrec_rank, so it couldn't be honored (Ben said scope to data we have).** (items 18-19) `64e539c` swipe header reworked — the top-right spyglass now opens ONE panel folding the old left-side "edhrec · commander" query chip AND the sort dropdown together: a full-width search field replaces the commander bar while open (item 18), sort orders (NAME/CMC/EDHREC) render as chips beneath with a direction arrow on the active one (item 19). (item 5) `092d0d0` APP STATE PERSISTENCE — App persists the active brew session (which legend's session is open) and restores it synchronously on mount, so backgrounding (Capacitor WebView reload) no longer dumps to Home; Brew's per-legend record now also stores the coarse brewView so a mid-review/mid-hand pass resumes there, not just swipe; cleared on every session exit; typed searches stay ephemeral by design so mid-search resumes to the default stack; 30-day TTL. **STILL OPEN: Ben's device pass over this whole batch (esp. the recolor on real OLED, the row mini-card + full-size overlay feel, the search+sort panel, the persistence resume after a real background kill); item 1 Moxfield UA email→Vercel env; the WREC chip-color steel decision (item 3 flag); and the long-standing SearchScreen/narrow-engine dead-code teardown + soft-corner doctrine + chip-form reimagining design session.** Prior: **2026-07-14 (latest) VAULT/SWIPE/REVIEW UAT BATCH SHIPPED — Ben's 12-item device-pass list (grounded in his Vault spec + arbiter-knowledge.md read for product context). Six commits on `main`, whole-project lint clean + prod build each, all verified lint+build only (no Supabase session in sandbox — DEVICE PASS OWED): (1) `8cdb70c` items 1–2 — the 47/100 counter is GONE from the decklist header (DECKLIST · N carries it); the WREC disclosure trigger moved into that freed header slot reading just `WREC ▾` (no numbers; an active filter is named by the section header; disclosure/tap-to-filter/re-collapse unchanged). (2) `3a900ca` item 3 — the >15-row group collapse from `430ea71` is fully REVERSED: type/mv/color groups always render every row (a–z flat unchanged). (3) `6556caf` item 4 — decklist rows strip to name (Zilla 600) + WREC chips + ×qty: type-line subline (and its "card data unavailable" fallback) and the amber cost badge REMOVED; dead displayType/manaOf/formatManaCost cleaned up. (4) `7ea2b4d` item 5 — Box detail WREC bars distribute across the full card-frame height (justify space-between — the dead space above/below is gone) and labels un-truncate: CARD-ADVANTAGE, MASS-DISRUPTION (local FULL_LABEL map in LegendIdentity; the five-cell band keeps its short labels). (5) `53926fa` items 6–9 — swipe top bar: sort button is now an icon-only spyglass (same sort-dropdown behavior; label/asc-arrow gone) and `done (N·N)` LEFT the top bar; DONE now sits bottom-right (old review slot) exiting to the deck list in BOTH brew and review/flip mode; the deck list's bottom-right relabeled review→done (still enters the review pass) so bottom-right = "I'm finished here" app-wide; the gesture reminder moved from below the card to the header band above it (right side; query chip capped to leave room, uncaps after fade) and FADES permanently after 5 swipe gestures/session. **Item 9 answered: brew swipe + mainboard review are ONE component (SwipeScreen + handMode) — no follow-up needed.** Dead fallout removed: onReview prop (SwipeScreen + Brew pass). (6) `f8af516` item 10 — the commander-bar subline never repeats the legend name and all stack counts are gone (`review:VOJA…`/`357 in stack`/`N in deck`/`N of M` → just `review` in flip mode, `deck flipped` at end, `reconnecting…` on retry, empty while brewing); dead fallout: totalStackCount prop + hand instance's stackOrigin. (7) `5b3e304` item 11 — placeholder → "use scryfall syntax" (picked over "search like scryfall": 19 vs 20 chars, safer at 375px). **Item 12 = NO-OP by diagnosis: `430ea71` already sorts AND groups simultaneously (type → header sections, a–z within; same mv/color) — the >15-row collapse was hiding it on device, and item 3's removal fixes the symptom.** STILL OPEN: Ben's device pass over all three 07-14 batches; the SearchScreen/narrow-engine dead-code teardown (stackNarrow chip is even more vestigial now); soft-corner doctrine amendment + full chip-form reimagining (own design session per the Vault spec's handoff note).** Prior: **2026-07-14 (later) VAULT LIST REDESIGN SHIPPED — Ben handed a full NN/g-grounded spec ("Vault List Redesign Spec" + reference `wrec-icons-preview.html` from Downloads, geometry/hexes folded verbatim into the repo). Four commits on `main`, lint + prod-build verified only (still no Supabase session in sandbox — DEVICE PASS OWED): (1) `fea5098` §1 — the five-cell WREC band in the decklist's frozen header collapses to a one-line `WREC · N tagged ▾` chip (N = cards with ≥1 tag, by quantity); tap discloses the shared WrecBand inline; picking a category filters AND re-collapses, and the collapsed chip then names the active filter + count in gold (tap-to-filter and the zero→"add more" gap flow both survive inside the disclosure). (2) `430ea71` §2 — group·sort merged into ONE view key (type / mv / color / a–z): type/mv/color group by that key with a–z inside; a–z = flat headerless list; any labelled group >15 ROWS first paints collapsed as header + "show all N" (session-scoped `openGroups`, deliberately not persisted); old `{groupBy,sort}` per-deck localStorage prefs migrate on read ("none"→a–z); review/flip order (Change 9) still follows display order incl. collapsed groups. Skipped by my call (flagged, Ben ok'd defaults): iOS sticky letter markers for a–z. (3) `bfcb4f4` §3+§5 — row hierarchy: name = Zilla Slab 600 15px primary ink; type line = quiet mono subline; mana cost moved OUT of the subline to a right-aligned amber mono badge (unbracketed per the reference HTML, beats the spec text's `[1G]`); rider: search placeholder → "search or add cards" (old copy truncated at 375px beside quick brew). (4) `ea265b8` §4 — the anonymous 6px tag dots are now 22px hard-corner ICON chips: chevrons=ramp, plus=card-adv, circle+slash=disruption, targets+slash=mass-dis, flag=plan; color by family (warm amber build-up #e8a020/#d4a838, cool green table-stop #7ab89a/#5a9d80, standout red plan #c0392b); max 2 chips + `+N` overflow; DASHED border = auto-suggested vs solid = user's (spec was silent on the auto/user split — my default, flag to Ben); glyphs live in WrecBand.jsx (`WrecIcon`, `WREC_CHIP_COLORS`) next to the taxonomy. NOT in scope (spec's own handoff note): soft-corner doctrine amendment + full chip-form reimagining (own design session); the WREC legend copy from the reference could later seed PileScreen's "WHAT IS WREC?" sheet. NEXT UNRESOLVED: Ben's device pass over BOTH 07-14 batches (v4 UAT + this vault redesign — esp. WREC chip disclosure, group collapse thresholds feel, icon legibility at 13px on device, two-door search bar, editable query chip) + the still-owed dead-code teardown of SearchScreen/narrow engine (see prior priority below).** Prior: **2026-07-14 v4 UAT BATCH SHIPPED — Ben ran migration 016 in Supabase + I re-ran `npm run ingest:cards` (38,233 processed → 34,616 upserted, 3,617 non-deckable skipped, cards table = 34,629 rows; non-deckable now filtered at ingest AND purged, so the token-name-collision bug from Change 8 is fully closed). Four commits on `main`, lint + prod-build verified only (sandbox has NO Supabase session → the brew/decklist/swipe/box screens are un-drivable here; DEVICE PASS OWED): (1) `3280b56` — quick brew folds into the decklist search bar's RIGHT half (empty+unfocused reads "quick brew" → deals the synergy stack via onBrew; focus or type morphs it to a search-submit magnifier → runGlobalSearch); the mid "brew" bottom-nav button is REMOVED (nav = back BL + review BR); hand→"review" verbiage (button label, swipe subline `hand:`→`review:`, gesture hint `← flip →`→`← browse →`). (2) `15cd83c` — Box detail pane (LegendIdentity) readout is now WREC BARS filling toward Rachel's targets (ramp10/cardadv12/disrupt12/massdis6/plan30); at/over target reads gold, zero dimmed. Dropped the name/type/mana/deck fields AND the numeric footer band. **Ben chose "bars only" in the Q, which CONFLICTS with his written "WREC shouldn't have replaced the deck bar" — so 47/100 LEFT the detail pane** (still shown on the box tile + decklist title); add a slim deck-completion bar back under the WREC bars if he misses it. (3) `761e12d` — decklist view (group/sort) control compacted INLINE onto the DECKLIST section header (a "tune" chip + expandable panel), killing one pre-list line; the WREC band + DECKLIST·N header STAY (Ben only greenlit compacting view-type — revisit if still heavy). (4) `aee259c` — editable QUERY CHIP on the swipe stack: a search stack shows its query, the synergy default shows "edhrec · <commander>"; tap → inline Scryfall box → submit re-queries via runGlobalSearch (still ANDs legal:commander + ci<=). Per Ben's Q1: synergy stays the default, editing switches to Scryfall (the chip just starts empty for the synergy default; color identity is applied server-side, so no `id<=` prefill needed). Origin dropped from the header subline (chip carries it). **REMAINING CLEANUP (its own commit — low-risk but touches Brew.jsx broadly, left out of the editable-chip commit to keep that additive/safe): the in-stack NARROW engine is now fully unreachable (no IA entry since Change 11 removed the swipe filter button; the editable chip supersedes it). Tear out: SearchScreen.jsx + the `brewView === "search"` render/backTarget branch/seed-fail `setBrewView("search")` fallback; parseNarrowTokens/cardMatchesNarrow/cmpNum/applyNarrow + the `narrow` param on buildSwipeCards; stackNarrow state + applyStackNarrow/clearStackNarrow/totalStackCount; and the SwipeScreen filter-chip + zero-match escape-hatch remnants + the four dead props (stackNarrow/totalStackCount/onClearFilter/onSearchAll). It's INERT today (never renders) but violates no-dead-code.** All v4 + prior v3 UI needs Ben's device pass — esp. the two-door search bar, the box WREC bars, swipe-to-delete (Change 14), the whole nav ladder (Change 11), and the new editable chip.** Prior: **2026-07-14 v3 UAT BATCH — 6 of 8 shipped (8,9,11,12,13,14 on `main`, unverified: sandbox has no Supabase session → screens un-drivable; lint + prod build + review only). Commits: Change 8 token/non-deckable pool fix (`bf235b0` — MANUAL STEPS OUTSTANDING FOR BEN: run migration `016_exclude_non_deckable.sql` in Supabase SQL editor = purge existing token rows + add layout guard to brew_stack/tag_stack, AND re-run `npm run ingest:cards` so future loads carry the layout/set_type filter); Change 9 review inherits decklist sort/group order (`2a45ea7`); Changes 12+13 Box WREC readout + tap-to-preview/tap-again-to-load, deck-row door removed (`d69d59d`, shared WrecBand component extracted); Change 14 decklist declutter — collapsible group/sort control + swipe-left-to-delete rows (`78d7ebc`, swipe gesture NEEDS DEVICE TEST); Change 11 nav ladder — back BL / review BR / home removed, ladder swipe→decklist→Box on both bottom buttons and hardware back, reviewOrigin state removed (`50944b4`). Change 5 (UB) stays KILLED. **NEXT UNRESOLVED — Change 10 (search consolidation) is BLOCKED on a design decision:** its editable query chip assumes every stack has an editable Scryfall query, but the default EDHREC stack is the brew_stack DB relevance ranking, NOT a Scryfall query. So editing the chip on the default stack must be either (a) NARROW the relevance stack client-side (keeps EDHREC order, matches Ben's 2026-07-04 "keep relevance" call) or (b) REPLACE with a Scryfall search (loses relevance). Ben must pick before building (the chip behaves differently by stack type either way). Change 10 also: folds Quick Brew into the decklist search bar's right half (empty+unfocused = "quick brew" deals EDHREC stack; focus/typing morphs it to a search-submit magnifier — Ben's flag-C answer), removes the mid "brew" button from the decklist nav (→ back BL + review BR only), and DELETES the standalone filter screen (SearchScreen narrowStack mode / brewView "search"). Change 15 (cosmetic) lands with/after 10: hand→"review" everywhere user-facing (button label still says "hand" pending this; origin subline `hand:`→`review:`; gesture hint `← flip →`→`← browse →`); its "filter icon-only" item is MOOT (Change 11 already removed the swipe filter button). All v3 UI needs Ben's device pass — esp. swipe-to-delete (14) and the whole nav ladder (11).** Prior: **2026-07-13 BATCH SHIPPED (rename + brew UX) — all on `main` + deploying to magikdex.vercel.app, NOT yet device/deploy-verified (this session's sandbox had no network egress → no Supabase session → the brew/decklist/swipe screens couldn't be driven; every change was verified by lint + production build + review only). Repo renamed mtg-dna→magikdex (app subdir, all refs, git remote → commander-zen/magikdex, Vercel Root Directory → magikdex, deploy confirmed green by Ben). Shipped one commit each: Change 1 decklist search bar → search-derived swipe stack (`02db9e6`, search stacks EPHEMERAL per Ben's (b) call — reopening a legend returns to the default EDHREC stack); Change 2 first-run empty state (`da8bb4b`); Change 3 swipe filter affordance + zero-results "search all cards" escape hatch (`29ed9da`); Change 6 decklist sort/group controls, Moxfield-familiar (`dac747a`); Change 4 HAND MODE (`b5b7f09`). Change 5 (UB-free toggle) KILLED PERMANENTLY by Ben ("out of scope, magikdex stays neutral on printings"). NEXT UNRESOLVED: (a) Ben device-verifies the batch — run the on-deploy checklist (add legend → decklist empty state → add-cards bar deals a `search:` stack → filter narrow shows chip + count → zero-match escape hatch → reopen returns to default stack → sort/group chips → HAND MODE from the decklist bottom-nav "hand" button); HAND MODE is the highest-risk/unverified piece (↑ cuts ALL copies out of the deck, ↓ maybes them, UNDO reverses; brew mode is gated byte-identical behind a `handMode` prop defaulting false). (b) Change 7 — the nightly-pipeline verification FINDING: there is NO automated data pipeline. All four `scripts/ingest-*.mjs` are MANUAL dev-machine-only (self-documented "NOT wired to any cron or deploy"); no Vercel cron, no GitHub Actions, no pg_cron. Failure mode = silent staleness (both the Scryfall `cards` cache and the EDHREC stacks drift with every set release, no alerting). Freshness check: `select max(updated_at), min(updated_at), count(*) from cards;` + same on `legend_synergy`/`legend_themes`. Building a scheduled ingest + a `last_synced` visibility surface is an APPROVED STANDALONE task (not part of this batch). Open design flags for Ben: Change 3 escape copy "nothing in this stack matches" also shows if you swipe THROUGH all filtered matches (minor); Change 6 `TYPE_ORDER` uses standard type precedence (Moxfield's exact live sub-order wasn't extractable from its SPA — one adjustable constant); Change 4 hand-mode cut is ALL-copies (cutting a basic cuts all copies — may want per-copy later). See Done 2026-07-13.** Prior app-store priority (still standing): **APP-STORE ROADMAP received from Ben 2026-07-08 ("i want magikdex to be an app. make it so") — Phase 2 Capacitor wrap is DONE on the code side (see Done 2026-07-08), and the 2026-07-09 nav restructure (deck list = landing surface, brew on its bottom nav, Box brew button gone — see Done) should ride along in Ben's device pass. Next unresolved: Ben installs Android Studio (the one machine prerequisite — this PC has no Java/Android SDK), then `npm run cap:android` to boot magicdex in an emulator/on-device and judge THE SWIPE inside the WebView — per the roadmap, if the flick feels wrong in the wrapper that's a BLOCKING bug, not polish. iOS project is generated + committed but building needs a Mac (or a cloud build service — Codemagic/Appflow decision pending); "Xcode is not installed" is the only `cap doctor` error. After the wrapped-swipe verdict, in order: service worker/offline (2.1 remainder — next code-side item, not gated on the verdict), Haptics + StatusBar plugins (2.3 — one-liners, do with the wrap verified), store mechanics (Phase 3: Apple $99/yr, Play $25, privacy policy page, Fan Content disclaimer). Cold-open onboarding walk (Phase 1.3) is ✅ DONE 2026-07-10 (see Done — full first-run journey verified end-to-end as a fresh anonymous user; one gate-count fix shipped `6fde9f6`; two notes added to Known Issues). appId `com.magicdex.app` — freely changeable until the first Play upload, PERMANENT after; appName kept ASCII "magicdex" for native tooling safety (brand "magıcdex" display-name polish later via strings.xml/CFBundleDisplayName).** Roadmap-vs-reality audit (this session): Phase 1.1 load-lag ✅ already root-fixed (`7d205b9`; device profiling still open), 1.2 deck CRUD ✅, 1.4 Box reorder ✅, 2.4 RLS/auth ✅ (013–015), PWA icons ✅ (`5796b23`); still open: 1.3 cold-open walk, 1.5 visual-language design session (needs Ben live, before store screenshots).
Prior priority (still standing, pre-share): **Ben's on-device pass — the platform is COMPLETE for sharing: multi-user auth live + verified, and universal-commander relevance LIVE (2026-07-03: migrations 013/014/015 all run; bulk EDHREC ingest finished — 3,286 of 3,321 commander candidates cached, 856,396 legend_synergy rows, 35 no-page fringe/false-positives; brew_stack v4 spot-verified on three never-curated commanders: Fire Lord Azula → Storm-Kiln Artist/Electrodominance/Archmage Emeritus, Muldrotha → Spore Frog/Seal of Primordium, Slimefoot → Utopia Mycon/Mycoloth with token-theme boost).** On his phone everything starts fresh (old test data purged; anonymous sign-in mints his new account silently). Spoiler season = re-run `npm run ingest:legend-edhrec -- --all` (service key lives in the gitignored .env now). Site URL nit: entered bare `mtg-dna.vercel.app` — change to `https://mtg-dna.vercel.app` if the dashboard kept it schemeless. Deferred candidate: on-demand Vercel function to ingest a brand-new spoiler commander at add-time (Ben has since said the manual script is all he wants — drop unless he re-raises). The backup-email nudge SHIPPED (see Done: 10-kept-cards prompt).
**NEW BLOCKER (Ben's device, 2026-07-03): "email rate limit exceeded" on backup-email sends — Supabase's BUILT-IN mailer is hard-capped (~2 emails/hour project-wide, not configurable without custom SMTP). It's a dev mailer, not production. Fix before sharing: Dashboard → Project Settings → Authentication → SMTP Settings → enable custom SMTP.** Cheapest paths: (a) Brevo free tier (300/day) — verify his gmail as a single sender, paste smtp-relay.brevo.com creds; (b) Gmail app-password SMTP (from his own gmail, ~500/day); (c) Resend needs a custom domain he doesn't have yet. After SMTP is live: Authentication → Rate Limits → raise emails/hour (unlocked once custom SMTP is set). Both app surfaces now render the rate-limit error as human copy instead of the raw message. The nudge itself is CONFIRMED working on his phone (fired at exactly 10 kept cards over the Azula brew).
Then: Ben's on-device pass of the full 2026-07-02 batch — **everything else is LIVE (migrations 011 + 012 both run, all verified end-to-end):** relevance v2+v3 stack (EDHREC synergy first, theme-weighted tag tail), new swipe chrome, auto-WREC tags (hollow chips; first deck-list open backfills untagged cards — intended), long-press Box slots (move/delete), and the WREC gap-fill flow (deck view → tap category → "add more" → category stack; verified live: RAMP on a GU test deck dealt 400 cards, Sol Ring first, flick wrote row + auto tag, session resumed as `wrec:ramp` after reload with decked cards excluded). "Why this card" (matched_tags/synergy in the swipe UI) is DEAD by Ben's call ("idgaf if people know why this card"). New legends still need `ingest:legend-tags` + `ingest:legend-edhrec` for a relevant stack. Remaining backlog:
1. **Arrange = drag** — Ben: "with arrange I want to click and drag instinctually." Long-press → move → tap-place shipped 2026-07-02; a true pointer DRAG remains a possible refinement — confirm with Ben whether tap-place satisfies.
2. **Loading speed** — partially addressed (detail pane now cache-first `29ddbac`; batched lookups `7d205b9`), but Ben still reports slowness on device; remaining suspects: Scryfall art_crop image loads over cell, the EDHREC-ordered seed query on brew entry. Profile ON DEVICE before more fixes.
3. **Verify on device**: the iOS keyboard status-bar bleed fix (`6f747c5`) and the paste tab's stale text after import (minor polish: clear on success).
Design-tier items from the original roadmap (chip form, soft corners doctrine, Moxfield categories) fold into this same conversation. The DB is clean-slate (migration 008 applied 2026-06-27) and the render crash that blanked the app on load against that empty state is now fixed (2026-07-01, `80e5b27` — see Done). Next: from a truly empty Box, add a legend (search + paste-deck import paths, both landed 2026-06-22/23), confirm it gates correctly at <100 cards and clears the gate at 100, brew a session against it (direct-to-swipe, flick-is-a-write, undo, in-session search staying in color identity), and confirm deck-row review + WREC tagging round-trip against the wiped `deck_cards`/`deck_card_tags` tables. **Hardware/browser Back climbs the one-rung ladder as the in-app chevron** (carousel→Box, search/review→carousel) — test on a real device since the history back-trap can't be exercised in the desktop preview; also re-verify safe-area insets clear the notch/home-indicator (no tab bar). Then: decide the pile's fate (carousel gesture model made it unreachable — see Known Issues); the non-legend brew modes (Import/Free Pile/Discovery) and the legacy Brew "shell" landing are UNREACHABLE (no tab) — decide whether to delete or resurface them.

## Done
- ✅ 2026-07-14 (device-pass UAT round 3) — **7 items from a full end-to-end deck build; the mana-base gap sent Ben to EDHREC** — all on `main`, lint clean + prod build each, device re-pass owed. ⚠️ Migration 017 still needs running. See Cold Start priority for full detail.
  - ✅ Targets (`fd8152a`) — WREC band reads count/target; no tint by request.
  - ✅ WREC calculus (`b5418b0`) — maybeboard excluded from both readouts.
  - ✅ Search draft (`3b5d5e3`) — typed Scryfall syntax survives backing out.
  - ✅ MDFC (`2f3d39f`) — front-face bucketing + "N including mdfc" on the LANDS header.
  - ✅ Lands door (`b7cc540`) — "+ lands" deals nonbasic lands for the colour identity. **The EDHREC-detour fix.**
  - ✅ Basics (`3915fda`) — − N + stepper on basic / "any number" rows.
  - ✅ Maybeboard killed (`d68032f`) — ↓ closes the carousel, back/done removed, move-path + inline grid gone, imports drop sideboards, migration 017 deletes the rows.
  - ✅ Anchor (`97f987a`) — closing the carousel lands on the card you were viewing.
- ✅ 2026-07-14 (device-pass UAT round 2) — **4 items; WREC consolidated to one tagging surface** — all on `main`, lint clean + prod build each, device re-pass owed. See Cold Start priority for full detail + the maybe-row asymmetry flag.
  - ✅ WREC copy (`5ad174c`) — "in honor of the great Rachel Weeks" + guidelines-not-rules.
  - ✅ Band key (`b86947f`) — role icons on the WrecBand composition strip.
  - ✅ Carousel tagging (`d14a637`) — WREC icon chips below the card in the review flip (hand mode only).
  - ✅ Grid retired (`e4b7bda`) — decklist row tap → carousel at that card; maybeboard keeps its inline expander.
- ✅ 2026-07-14 (Ben's device-pass UAT) — **6 observations from a live device pass** — all on `main`, lint clean + prod build each, device re-pass owed. See Cold Start priority for full detail.
  - ✅ Deck count (`ea4836b`) — deckTotal excludes maybe/pile; count is mainboard + commander everywhere.
  - ✅ Search staleness (`225c696`) — a removed card becomes searchable again (prune existingCardRows on last-copy removal).
  - ✅ Review-at-card (`a913397`) — mini-card + 'review' open the swipeable carousel at the selected card; static overlay removed.
  - ✅ Deeper-black rows (`2fdff68`) — idle rows transparent (opaque bg only while swiping); no more GPU-layer darkening.
  - ✅ WREC (?) (`9bf9764`) — in-app explainer (WREC = Wachel Reeks Effectiveness Coefficient + 6 roles) with the video link at the bottom.
  - 📋 Two non-ticket follow-ups still owed: (A) share-ready pass (Vault form recovery + Vercel env vars); (B) preprod/staging workflow.
- ✅ 2026-07-14 (Sonnet-UAT batch 3) — **Ben's 7-item bug list** — all 7 on `main`, lint clean + prod build each, verified lint+build only; device pass owed. See Cold Start priority for full per-item detail.
  - ✅ Item 1 (`a0075df`) — paste-deck sheet auto-closes 1.2s after a successful import.
  - ✅ Item 2 (`57071d7`) — WREC tagging grid shows icon badges with a real applied/empty state; fixed the stale-`had` toggle that left deselected cells stuck filled.
  - ✅ Item 3 (`c9dffce`) — flip control only for true DFCs (real back-face image); split/aftermath read by phone rotation.
  - ✅ Item 4 (`83e48b2`) — deck card count subline under the commander name (commander + mainboard).
  - ✅ Items 5-6 (`8b381df`) — WREC (?) opens the Command Zone guide (clean youtu.be URL; dropped Ben's malformed `?is=` param).
  - ✅ Item 7 (`d450d40`) — "All Cards Seen" drops the dead pile; VIEW DECK + counts only.
  - 📋 Two non-ticket follow-ups flagged, NOT built: (A) share-ready pass for a Bluesky contact (Vault form recovery + missing Vercel env vars); (B) preprod/staging workflow.
- ✅ 2026-07-14 (Sonnet-UAT batch 2) — **Ben's 19-item Sonnet-session list** — 18 shipped in 11 commits on `main`, lint clean + prod build each, verified lint+build only; device pass owed. Item 1 blocked (Moxfield UA email). See Cold Start priority for full per-item detail.
  - ⛔ Item 1 — Moxfield import 403: NOT code-fixable; `api/deck.js` MOXFIELD_UA path already wired, needs Ben's Moxfield-support UA → Vercel env.
  - ✅ Item 4 (`8205c96`) — exclude-lands seed default OFF.
  - ✅ Items 6+8 (`f7ae278`) — decklist header IS the WREC trigger (just `WREC ▾`); disclosure stays open on category select.
  - ✅ Item 7 (`320bf8d`) — small legend card thumbnail in the review header corner.
  - ✅ Item 9 (`9824c7c`) — real H1/H2 header hierarchy (Zilla 700 sections, mono 13 groups).
  - ✅ Item 10 (`a1b3ab3`) — Box WREC bars split the frame evenly; CARD-ADVANTAGE/MASS-DISRUPTION never wrap.
  - ✅ Item 15 (`dd38d9d`) — done→`review`, resumes the flip pass at the last-viewed card.
  - ✅ Items 16+17 (`ff4312e`) — gesture hint centered; "mainboard"→"deck".
  - ✅ Item 2 (`301fccc`) — light mode killed (flat palette, no toggle, 12 shell files stripped).
  - ✅ Item 3 (`aaa7a22`) — Jackson Storm steel recolor (#08090c/#12151a + #38bdf8); amber/green keys renamed accent/accent2. **WREC chip colors left categorical — Ben's call to restyle.**
  - ✅ Items 11-13 (`78977d1`) — row expands to a mini-card + WREC tag targets; mini-card taps to full-size; tag icon persists; oracle block removed. "6 tags" = 5+move grid, not a new category.
  - ✅ Item 14 (`edfd479`) — Moxfield two-axis Group By × Sort By (EDHREC-rank sort omitted: no edhrec_rank in the deck-row cache columns).
  - ✅ Items 18-19 (`64e539c`) — spyglass opens a full-width search + sort chips; old query chip and sort dropdown folded in.
  - ✅ Item 5 (`092d0d0`) — active brew session persists + restores after a background reload; per-legend record stores the coarse view.
- ✅ 2026-07-14 (latest) — **Vault/swipe/review UAT batch (Ben's 12-item device-pass list)** — six commits on `main`, lint + prod-build verified only; device pass owed. See Cold Start priority for full per-item detail.
  - ✅ Items 1–2 (`8cdb70c`) — decklist header count gone; `WREC ▾` trigger (no numbers) in its slot.
  - ✅ Item 3 (`3a900ca`) — group collapse reversed; grouped sections always render in full.
  - ✅ Item 4 (`6556caf`) — rows = name + WREC chips + ×qty; type subline + cost badge removed.
  - ✅ Item 5 (`7ea2b4d`) — Box WREC bars fill the frame height; CARD-ADVANTAGE / MASS-DISRUPTION un-truncated.
  - ✅ Items 6–9 (`53926fa`) — spyglass sort icon; done → bottom-right (both modes + deck list, no counts); reminder above card, fades after 5 swipes. Brew swipe + review pass = ONE component (SwipeScreen/handMode).
  - ✅ Item 10 (`f8af516`) — legend name once; subline = review / deck flipped / reconnecting / empty; stack counts gone.
  - ✅ Item 11 (`5b3e304`) — placeholder "use scryfall syntax".
  - ✅ Item 12 — NO-OP: sort+group simultaneity already shipped in `430ea71`; the collapse (now removed) had been masking it.
- ✅ 2026-07-14 (later) — **Vault list redesign (Ben's NN/g spec + wrec-icons-preview.html reference)** — four commits on `main`, lint + prod-build verified only; device pass owed. See Cold Start priority for full detail.
  - ✅ **§1 WREC strip → summary chip** (`fea5098`) — five cells deferred behind `WREC · N tagged ▾`; disclosure keeps tap-to-filter + the zero-gap "add more" flow; filtered state named on the collapsed chip.
  - ✅ **§2 one view key + group collapse** (`430ea71`) — type/mv/color/a–z single control (old {groupBy,sort} prefs migrate); labelled groups >15 rows paint collapsed with "show all N" (session-scoped).
  - ✅ **§3+§5 row hierarchy + placeholder** (`bfcb4f4`) — Zilla 600 name, quiet type subline, right-aligned amber cost badge; placeholder "search or add cards" fixes 375px truncation.
  - ✅ **§4 icon chips** (`ea265b8`) — dots → 22px icon chips, color-by-family/icon-by-category, max 2 + `+N`, dashed=auto vs solid=user (my default — spec silent); glyphs verbatim from the reference, co-located in WrecBand.jsx.
- ✅ 2026-07-14 — **v4 UAT batch (from Ben's live device pass) + Change 8 manual steps CLOSED** — all on `main`, lint + prod-build + review verified only (no Supabase session in sandbox); device pass owed. See Cold Start priority for the full detail + the remaining narrow-engine/SearchScreen cleanup.
  - ✅ **Ingest re-run + migration 016** — Ben ran `016_exclude_non_deckable.sql` (purge + brew_stack/tag_stack layout guard); I re-ran `npm run ingest:cards` (34,616 upserted, 3,617 non-deckable skipped, cards table 34,629). Token-name-collision bug fully closed.
  - ✅ **Change 10 quick-brew + two-door search bar** (`3280b56`) — decklist "add cards" bar: right half is "quick brew" while empty+unfocused, becomes a search magnifier on focus/type; mid "brew" nav button removed. hand→"review" verbiage (Change 15).
  - ✅ **Box WREC bars** (`15cd83c`) — LegendIdentity readout = five bars filling toward Rachel's targets; name/type/mana/deck + numeric footer band gone. Ben's "bars only" pick dropped 47/100 from the detail pane (flag if he wants a deck bar back).
  - ✅ **Decklist declutter** (`761e12d`) — group/sort control compacted inline onto the DECKLIST header (tune chip + expandable panel).
  - ✅ **Editable query chip** (`aee259c`) — swipe stack identity is a tappable chip; tap → inline Scryfall search → runGlobalSearch. Synergy stays default, editing switches to Scryfall (Q1).
  - ⏭️ **Remaining:** delete SearchScreen + tear out the now-inert in-stack narrow engine (see priority). ❌ Change 5 stays killed.
- ✅ 2026-07-14 — **v3 UAT batch, 6 of 8 (from Ben's live device testing)** — all on `main`, lint + prod-build + review verified only (no session in sandbox); device pass owed. See Cold Start priority for the full per-change detail, the Change 8 manual steps (run migration 016 + re-ingest), and the Change 10 blocker.
  - ✅ **Change 8** (`bf235b0`) — tokens/non-deckable excluded at ingest + migration 016 purge + brew_stack/tag_stack layout guard. Root cause: getCardDataBatch last-row-wins on a token sharing a real card's name.
  - ✅ **Change 9** (`2a45ea7`) — review/flip stack inherits the decklist's current sort/group order.
  - ✅ **Changes 12+13** (`d69d59d`) — Box detail pane shows the deck's WREC composition (shared WrecBand component) instead of the redundant name·count line; tap-to-preview / tap-the-selected-again-to-load; deck-row door removed.
  - ✅ **Change 14** (`78d7ebc`) — decklist group/sort collapse to one expandable control; per-row ✕ replaced by swipe-left-to-delete (option A; gesture needs device test).
  - ✅ **Change 11** (`50944b4`) — back BL / review BR / home removed on decklist + swipe; ladder swipe→decklist→Box (bottom buttons + hardware back aligned); dead reviewOrigin removed.
  - ⏸️ **Change 10** — BLOCKED on the editable-chip vs default-relevance-stack design decision (see priority). ❌ **Change 5** stays killed. **Change 15** bundles with 10.
- ✅ 2026-07-13 — **Repo rename + brew-UX batch (Changes 1–4, 6; Change 5 killed; Change 7 report-only)** — all on `main`, deployed to magikdex.vercel.app, verified by lint + production build + review only (sandbox had no network egress → no Supabase session → screens un-drivable; DEVICE/DEPLOY VERIFICATION STILL OWED, esp. hand mode):
  - ✅ **Rename mtg-dna → magikdex** (`66b28df`): `git mv` of the app subdir (history preserved as renames); functional refs updated (`.claude/launch.json` `--prefix magikdex`, `package.json`/`package-lock.json` name); `HARDENING.md` path pointers. git remote → `commander-zen/magikdex` (owner was already commander-zen; only repo name was stale). Ben set Vercel Root Directory → `magikdex` and confirmed the deploy green. **Deliberately LEFT**: the `mtgdna-theme` localStorage key (renaming resets every user's saved theme) and SESSION_STATE historical log entries. Capacitor appId (`com.magikdex.app`) + on-device display names were ALREADY magikdex — native/store bundle unaffected.
  - ✅ **Change 1 — decklist search bar → search-derived swipe stack** (`02db9e6`): an "add cards · name or scryfall syntax" bar on the decklist (below the WREC band) builds a custom stack from ALL of Scryfall, hard-constrained server-side of the user's input to `legal:commander` + the deck's `ci<=`; results become the swipe stack (never a list), routed into the same SwipeScreen; cards already in the deck are skipped; header subline reads `search: {query}`. Bare text = name search (3+ chars, submit-to-build, NO debounce per Ben); operators (`: = < > "`) pass through as raw syntax. New `runGlobalSearch` sets `baseStackRef` so the filter (Change 3) narrows the search stack, and IS the alternate-stack-source primitive hand mode reuses. **Ben's (b) call: search stacks are EPHEMERAL** — not persisted as the resume seed, so reopening a legend returns to the default EDHREC stack.
  - ✅ **Change 2 — first-run empty state** (`da8bb4b`): empty deck (0 beyond commander) shows a Zilla `{N} cards are shuffled and waiting` line (N = dealt stack size), a mono support line, and a gold `start brewing →` CTA (same `onBrew` as the bottom-nav); the bottom-nav brew label goes gold while empty. Replaces the lone `—` (which read as broken); a filtered-empty section keeps its `—` (different meaning). Clears at ≥1 non-commander card.
  - ✅ **Change 3 — swipe filter affordance + escape hatch** (`29ed9da`): magnifying glass → funnel `filter_alt` icon + `filter` label (it narrows the current stack, never searched all cards — that false promise was the dead end); persistent gold `filtering: {query}` chip with ✕ (`clearStackNarrow`); header count reads `{matching} of {total} in stack`; narrow panel copy → `narrows the current stack of {N}`. A zero-match filter now LANDS ON the swipe with a `nothing in this stack matches` state + gold `search all cards →` that re-runs the query through `runGlobalSearch` and swaps in the bigger stack — the dead end becomes a bigger stack, user never leaves the swipe.
  - ✅ **Change 6 — decklist sort/group controls** (`dac747a`): group-by (type / mana value / color / none) + sort (a–z / mana value / color) as a compact mono chip row kept clear of the WREC band. Default group-by-type in a Moxfield-familiar order (single adjustable `TYPE_ORDER`), sort a–z; per-group counts; maybeboard too. Buckets derive from already-loaded `cardData` (no new fetch); unresolved cards → Other/MV ? tail. Persists per deck (localStorage keyed by legend id). Pure read/organize — row interactions unchanged.
  - ✅ **Change 4 — HAND MODE** (`b5b7f09`, the big one): reuse SwipeScreen with the deck as the stack (2nd consumer of the alternate-source primitive, not a fork). ← → flip, ↑ cuts ALL copies out of the deck, ↓ maybes them (down=always maybe, up=always resolve-out-of-the-held-stack). Entry = a `back_hand` "hand" button in the decklist bottom nav, shown once the deck has cards (4b). Misswipe safety = the existing history/UNDO reverses a cut (restores copies) or maybe (moves back) (4a). Header origin `hand: {deck name}`. Cut/maybe write through the same deck paths review uses (live counters); hand cards are a separate snapshot so the brew stack + resume seed are untouched. **Every hand-mode branch is gated behind a `handMode` prop defaulting false → brew mode byte-identical.**
  - ❌ **Change 5 (UB-free toggle) KILLED PERMANENTLY** by Ben — "out of scope, magikdex stays neutral on printings; fun & frictionless is the bar." Nothing built. (Predicate had been verified as `-is:universesbeyond`; also flagged that the default EDHREC stack is DB-served so a Scryfall predicate couldn't cover it anyway — moot now.)
  - 📋 **Change 7 (nightly pipelines) — REPORT ONLY, findings became a standalone task**: NO automated pipeline exists (see Cold Start priority for the finding + freshness queries + minimal-fix recommendation).
- ✅ 2026-07-10 — **Cold-open onboarding walk (app-store roadmap Phase 1.3)** — the true first-run experience walked ruthlessly as a brand-new anonymous user (fresh account minted on load, verified `is_anonymous`), plus a roadmap-vs-reality re-audit for Ben's "review and execute" prompt:
  - ✅ **The journey works end-to-end with one obvious action at every step**: empty Box shows "BOX EMPTY / add your first legend below" and the add tile is the ONLY interactive element in the tray → search shows identifiable results (set + type line) → select lands the legend, detail pane fills (name/type/mana/deck) → deck row (the one door) opens the empty deck list → BREW deals a 400-card EDHREC-ordered stack (test legend Krenko: Goblin Warchief/Skirk Prospector up top — universal relevance confirmed on a fresh user's first brew, zero curation) → gesture hint present → HOME returns. Zero console errors; both test legends deleted after (Krenko + a second added to verify the fix below; account box left empty).
  - ✅ **Gate-count jump fixed (`6fde9f6`)**: a deckless legend showed 0/100 in the Box badge + detail pane, then flipped to 1/100 the moment the deck list was first opened (Brew lazily creates the deck row; deckTotal counts the commander). Both no-deck fallbacks now start at 1 — verified live: a never-opened legend reads 1/100 in slot badge, DECK field, and deck row alike. Lint clean, build passes.
  - ⚠️ **Two findings filed under Known Issues** (closed sheets not inert — keyboard/AT only, no mobile impact; empty deck list lacks first-run copy — design-tier, Ben's call). The desktop browser pane's renderer froze this session (CSS transitions never progressed, screenshots timed out — tool-side, not the app: DOM/inline styles/network all updated correctly), so the walk was verified at the DOM level; the visual layer rides on Ben's device pass as already planned.
- ✅ 2026-07-09 (later) — **Deck import by URL (Moxfield + Archidekt)** per Ben ("the initial build was to paste… so tags would transfer; now we auto tag" — tags deliberately do NOT transfer on import; the tagged export is unchanged):
  - ✅ **One box, two doors**: the existing paste textarea auto-detects a moxfield.com/decks or archidekt.com/decks URL (`isDeckUrl`) and goes through `prepareImportFromUrl`; anything else parses as pasted text exactly as before. Placeholder now leads with the URL form.
  - ✅ **New `/api/deck` Vercel function** (api/deck.js, ESM — package `type:module` governs api/ too, NOT the usual CommonJS): both providers block browser CORS (verified live: Moxfield sends no ACAO; Archidekt pins ACAO to localhost:3000), so the function fetches server-side and returns a normalized few-KB `{name, commanders, cards:[{name,quantity,section}]}` instead of the provider's ~500KB blob. ACAO * (public read-only data; the Capacitor shell isn't the Vercel origin). Edge-cached 5 min. Payload shapes verified against LIVE decks before building (Moxfield v3 boards.\*.cards; Archidekt cards[].categories + deck categories includedInDeck flags).
  - ✅ **Sections ride along**: provider maybeboard/sideboard → `maybe`; LegendBox's import insert now honors `line.section` (pasted text still defaults decklist).
  - ✅ **Verified end-to-end in preview against the DEPLOYED function**: Archidekt URL → 81 resolved / 0 unresolved, commander auto-detected (Xyris, the Writhing Storm) → imported 80 cards · 0 tagged → deck list opened at 100/100 and the heal auto-tagged (13 RAMP / 25 CARD-ADV) — the auto-tag-instead-of-transfer thesis works. Test legend deleted; zero console errors.
  - ⚠️ **Moxfield is 403-blocked at their bot filter** (verified: adaptive Cloudflare scoring; blocks Vercel datacenter IPs / server UAs — their policy is pre-approved User-Agents for tools). The app surfaces it as "moxfield refused the request (bot filter) — paste the deck text instead" (verified live). **Ben action item: email Moxfield support for an approved User-Agent, then set it as `MOXFIELD_UA` in Vercel env — the function already reads it.** Archidekt has no such gate and works today.
  - 🔎 **Domain correction discovered en route: production is `https://magikdex.vercel.app`** — mtg-dna.vercel.app returns DEPLOYMENT_NOT_FOUND (and magicdex.vercel.app is an unrelated stranger's project). The Supabase Site-URL nit in the old Cold Start priority should be re-checked against the magikdex domain. Client API base + function UA updated accordingly.
- ✅ 2026-07-09 (later) — Box detail pane: STATUS field removed per Ben — the readout is now name / type / mana / deck. The decks fetch keeps its `status` column (same embed contract as the shared legendDeck.js resolver). Verified in preview on a disposable legend (readout labels = exactly those four, no console errors; deleted after, Box empty). Note: `decks.status` still exists in the schema and is written on deck create ("Active") — it's just no longer displayed anywhere; if it stays unused, dropping the column is a future schema-cleanup candidate for Ben to call.
- ✅ 2026-07-09 (later) — Ben's device-pass verdicts on the new flow ("new brew flow is way better love it") + three fixes, all verified live in preview on a disposable Krenko legend (deleted after, zero console errors, lint+build clean):
  - ✅ **Home is one thumb position app-wide**: deck-list bottom nav swapped to BREW left / HOME right — home now sits bottom-right exactly like the swipe screen's home (Ben: "home needs to be in the same spot"). Verified by bounding box (brew x≈81, home x≈410).
  - ✅ **Tag chips are a plain fill/empty toggle** (Ben: "empty to blank if it is the pre tagged recommendation; fill if it is empty") — the 2026-07-03 confirm ladder is DEAD by his call: tapping a dashed auto chip now REMOVES it (was: confirmed it solid), tapping a blank chip fills it as user's. `confirmAutoTag` deleted from deckTags.js (no dead code). Verified: dashed RAMP → tap → blank → tap → filled amber; blank PLAN → tap → filled.
  - ✅ **Off-swipe art warming** (the load-time complaint): the queue DATA already seeded silently on deck-list entry (shipped with the restructure); now the first ~6 slots' card art prefetches into the browser cache while the user reads the deck list (`warmedArtRef` effect in Brew.jsx; SwipeScreen's own next-3 preloader owns it after mount). Verified: 8 cards.scryfall.io requests fired while still on review, before brew was ever tapped.
  - ⚠️ Known trade-off now more visible: the deck-open heal re-suggests auto tags for cards with ZERO tags — so clearing the ONLY tag on a card brings the dashed suggestion back next deck-list open (pre-existing heal behavior, documented 2026-07-02). If that annoys Ben on device, the fix is a per-card "suggestions dismissed" marker — needs his call.
- ✅ 2026-07-09 — Brand spelling is **magikdex** (Ben: "like the URL") everywhere user-visible: wordmark (dotless ı kept — magıkdex), tab title, apple-mobile-web-app-title, manifest name/short_name, settings colophon, Capacitor appName, Android app_name/title_activity_main, iOS CFBundleDisplayName, ingest UA strings. **App id moved too while it's still free: `com.magikdex.app`** (gradle namespace/applicationId, java package dir renamed, custom_url_scheme, iOS PRODUCT_BUNDLE_IDENTIFIER ×2 — BOM stripped from pbxproj after the PS edit). Verified in preview: title/wordmark/colophon all k-spelled, zero stale spellings in rendered text; build+sync+lint clean. **localStorage keys deliberately NOT renamed** (magicdex-box-order, -last-legend, -brew-session:, -backup-nudge:, -brew-defaults, magicdexBrew history marker): renaming would silently wipe every existing user's box order/sessions/settings — internal, never displayed. Also untouched: the SVG source filename (magicdex-dex-v3.svg) and README's wordmark — not app surfaces; rename README's if wanted.
- ✅ 2026-07-09 — Deck list is the landing surface; brew moved off the Box (Ben's UX call: Moxfield/Archidekt familiarity — select a commander → see the decklist; brew deals from there):
  - ✅ **Box detail pane (LegendIdentity)**: the `brew` button is REMOVED — the deck row is now the only door, ALWAYS tappable (was inert with no deck; Brew.jsx already creates the deck lazily on session start, so "no deck yet" opens the empty deck list — a fresh legend is never stranded). Row minHeight 36→44 (it's now the primary target; 44px floor). Footer's column-gap wrapper simplified away with the button — no layout gap left.
  - ✅ **Deck-list bottom nav (ReviewScreen)**: LEFT is now HOME (icon+label, exits to the Box — muted color), RIGHT is now BREW (label + `style` card-stack icon, emphasized `--text` color — the forward action). Props renamed `onBack`/`onHome` → `onHome`/`onBrew`; Brew.jsx maps onBrew→`goToSwipe`, onHome→`goHome` (both pre-existing functions — zero new routing).
  - ✅ **One brew path confirmed, not a parallel one**: review-as-landing already seeds the swipe queue in the background (`resumeOrSeedSwipeQueue` with `setView:false`), so the nav's BREW deals the exact same stack the old Box button did — verified live: Krenko test legend → deck row → empty review → BREW → "400 IN STACK" EDHREC-ordered relevance stack.
  - ✅ Untouched by design: hardware-back ladder (`reviewOrigin` semantics unchanged), WREC band, export, delete row, swipe screen chrome, non-live (paste-import) review which has no bottom nav.
  - ✅ Verified live end-to-end in preview as a fresh anonymous user (add Krenko → deck row lands on review with auto-created deck → BREW deals stack → done → HOME exits to Box → Box shows NO brew text anywhere, leaf-node scan = 0) — test legend deleted after, DB clean, zero console errors. Lint clean; build passes.
- ✅ 2026-07-08 — Capacitor wrap (app-store roadmap Phase 2.2 + native icon/splash from 2.1): magicdex is now a wrappable native project on both platforms:
  - ✅ `@capacitor/core|cli|android|ios` 8.4.1 installed; `capacitor.config.json` (appId `com.magicdex.app`, appName `magicdex`, webDir `dist`, backgroundColor `#1a1410` both platforms, iOS `contentInset: never` — the app already handles safe areas via `env()` + the existing `viewport-fit=cover` meta).
  - ✅ `npx cap add android` + `npx cap add ios` generated both native projects; `npx cap sync` verified clean end-to-end (web assets → `android/app/src/main/assets/public` + `ios/App/App/public`); `cap doctor`: "Android looking great 👌", only error = Xcode missing (expected on Windows).
  - ✅ Native icons + splash: `scripts/gen-icons.mjs` extended (same one-source-of-truth SVG) to emit `assets/icon.png` (1024 full-bleed dark) + `assets/splash.png`/`splash-dark.png` (2732 dark field, mark centered ~30%); `npx @capacitor/assets generate` populated all Android mipmap/adaptive-icon/splash densities (100 files) + iOS AppIcon/Splash (13 files). Pixel-inspected the xxxhdpi launcher and port-xhdpi splash — device mark renders correctly (gold lens on dark, no garbling).
  - ✅ Repo hygiene: Capacitor's own `.gitignore`s exclude copied web assets + build outputs (native projects are commit-safe); eslint `globalIgnores` gained `android`/`ios` (the only JS inside is the copied gitignored bundle — lint was choking on it); new scripts `cap:sync` (build+sync) and `cap:android` (build+sync+open Android Studio). Lint CLEAN, build passes.
  - ⚠️ **Not verifiable on this machine**: no Java/Android SDK → no emulator boot (Ben: install Android Studio, then `npm run cap:android`); iOS build needs a Mac. The wrapped-WebView swipe-feel test is THE gate before anything else in Phase 2.
- ✅ 2026-07-04 — Lint is now CLEAN (`npx eslint .` = 0 problems, down from the long-standing 37→30 baseline; `npm run build` passes). Two kinds of fix:
  - ✅ **Real dead-code removal** (per "no dead code left behind"): `legends` write-only state in Home.jsx; `passBg` (PileSwipeScreen), `searchFocused` + its onFocus/onBlur (SearchForm — value never read), `cardExpanded` state + its effect-reset + the no-op tap-to-toggle (SwipeScreen), `onGoToBrews` + uncalled `isCommanderEligible` (SwipeScreen), `onDoubleTag` prop (PileScreen), and `brewMode` write-only state + its two setters (Brew.jsx). Biggest one: PileScreen's entire **WREC/BRACKET scoring subgraph was fully dead** — the two on-screen displays had been commented out, so `bracket`, `wrecScoreStr`, `wrecScoreColor`, `wrecScore`, `wrecBar`, `basicIds`, the `computeWrecScore` helper, and the `WREC_CATEGORIES`/`WREC_TARGETS` imports only fed commented-out JSX. Removed the computation chain AND the two stale commented blocks. **Kept** the live "WHAT IS WREC?" info sheet + `WREC_INFO` + `WREC_CHIP` (still used by the per-row tag dots) — those aren't dead. Empty `catch {}` blocks (PileScreen ×3, SwipeScreen ×1 haptics/pointer-capture) got explanatory comments.
  - ✅ **Scoped `eslint-disable` for legitimate React patterns** (refactoring core-flow files for a style rule would risk regressions Ben didn't ask for): 14 `react-hooks/set-state-in-effect` on genuine reset/sync/data-load effects (per-card face/visual reset in CommanderModal/PileSwipeScreen/SwipeScreen, on-open sheet resets in CommanderSearchSheet/AddLegendSheet, debounced-search `setResults([])`, LegendBox `loadLegends`/page-clamp, LegendIdentity stale-clear, useGameChangers cache-hit, SearchScreen progress bar, Brew session-init) — each with a one-line why; the Brew session-init `exhaustive-deps` warning; and ThemeContext's `react-refresh/only-export-components` on `useTheme` (co-located with the provider on purpose — splitting it would churn every import).
  - ⚠️ **Non-observable in the desktop preview** — every change was provably dead code or a lint-suppression with identical runtime behavior, so build + lint passing IS the proof (no browser path changed). Nothing here needs a device pass; the icon/search items below still do.
- ✅ 2026-07-04 — In-swipe search now NARROWS the relevance stack instead of running a generic search (Ben: mid-brew he wanted to drop Eldrazi from Zhulodok "as I was searching" — the box was blank because the default stack is the `brew_stack` DB ranking, not a Scryfall query). His call when asked: **keep relevance** (narrow in place) over prefill-then-plain-search.
  - ✅ The legend-session search box is now a CLIENT-SIDE filter over the current relevance/wrec stack, preserving EDHREC ordering — cards carry `type_line`/`oracle_text`/`cmc`/`name` from the cache, so no RPC change was needed. Supported terms (AND'd): `t:`/`-t:` (type line — the Eldrazi case), `o:`/`-o:` (oracle text), `cmc`/`mv` + `>= <= = > <`, and bare/negated words matched against name∪type∪text. New module-level `parseNarrowTokens`/`applyNarrow` in Brew.jsx.
  - ✅ Architecture: new `stackNarrow` state (the filter) kept SEPARATE from `query` (the stack identity: ""/`-t:land`/`wrec:cat`); new `baseStackRef` holds the full un-narrowed stack so clearing the filter restores everything (not a shrunk remainder). One `buildSwipeCards(base, narrow, order, dir)` helper now feeds seed, resume, add-more, sort, and narrow so they all build the queue identically. Narrow persists in the per-legend brew session and re-applies on resume. Sort on a legend session re-derives from the base (never a generic re-search that would drop the ranking/narrow).
  - ✅ SearchScreen gained a `narrowStack` mode (on for legend sessions): otag chips hidden (can't be evaluated client-side), lands toggle hidden (land inclusion is fixed by the seed), empty box is valid = "SHOW ALL" (clears the filter), button reads FILTER/SHOW ALL, hint line explains it filters in place. Non-session (Loki/legacy) search is unchanged. Box now prefills with the active narrow, so reopening shows the current filter (was always blank).
  - ✅ Verified: `npm run build` clean; lint at the 37 baseline (0 new); app loads (module transforms 200, root renders); 9/9 unit cases on the narrow parser pass incl. `-t:eldrazi`, `-t:eldrazi -t:land`, `cmc<=3`, `mv>6`, `o:annihilator`, bare-word, empty=identity.
  - ⚠️ **Needs Ben's device pass** — the full legend→swipe→search→narrow path can't be driven in the desktop preview (needs anonymous auth + a seeded legend stack). Test: brew a legend, tap the swipe search icon, type `-t:eldrazi`, FILTER → stack should lose Eldrazi but keep EDHREC order and position resets to top; empty box + SHOW ALL restores; reopen search shows the active filter prefilled; reload resumes the narrowed stack.
  - Follow-ups noted (not built): (1) narrowing can't surface a card OUTSIDE the top-N relevant stack — deliberate per "keep relevance"; if Ben wants a "search all cards" escape hatch that's a separate mode. (2) No badge on the swipe screen showing an active filter yet — only the prefilled box reveals it; add a small "filter: …" chip if he wants it visible mid-swipe.
- ✅ 2026-07-04 — Favicon + PWA icons replaced with the magıcdex device mark (`5796b23`) — closes the manifest icon gaps flagged back in `167de05`:
  - ✅ Canonical source committed at `src/assets/magicdex-dex-v3.svg` (flat vectors — rounded body, lens with mana diamond, panel-line step, graduated accent dots, play triangle, base line; NO blur filters, so it rasterizes crisply). Ben saved it to Downloads; I could not find any `magicdex*`/`dex-v3*` SVG in the repo or on disk, so I asked and he pasted the path.
  - ✅ New `scripts/gen-icons.mjs` (`npm run gen:icons`) rasterizes ALL assets from that one source via `sharp` + `png-to-ico` (added as inner-project devDeps). Outputs to `public/`: `favicon.svg` (verbatim source), `favicon.ico` (16/32/48 multi-res), `icon-192.png` + `icon-512.png` (`purpose:any`, transparent rounded corners), `icon-maskable-512.png` + `apple-touch-icon.png` (180) — both full-bleed dark `#1a1410` so iOS/Android corner masks have no transparent gap; interior art keeps its ~9% inset = maskable safe zone.
  - ✅ `index.html`: added `<link rel="icon" ico>` alongside the svg, and repointed `apple-touch-icon` from the old svg to `/apple-touch-icon.png`. `manifest.webmanifest`: icons now list svg(any)+192(any)+512(any)+512(maskable); name/short_name already "magicdex".
  - ✅ Verified: `npm run build` clean, all 6 assets + manifest land in `dist/`; dev server serves each at 200 with correct content-types and all four `<link>` tags resolve (checked via preview fetch). Pixel-inspected 16/32/48/180: lens+diamond anchor legible at every size; only the three 16px accent-dots smear into one gold smudge (unavoidable at 16px, doesn't hurt recognizability — modern browsers pull the 32px/SVG). No linework simplification needed.
  - ⚠️ **iOS "Add to Home Screen" can't be verified in the desktop preview** — needs Ben's device: open the deployed site in **Safari** (not Chrome — only Safari honors `apple-touch-icon`), Share → Add to Home Screen, confirm the tile shows the dark mark with gold lens (not a screenshot of the page). If iOS caches an old icon, remove the tile and re-add, or bump the apple-touch-icon path with a `?v=2` query.
  - Cleanup note: the first `npm install` ran from the OUTER repo root and created a stray `package.json`/`package-lock.json`/`node_modules` there — removed them and reinstalled into `mtg-dna/mtg-dna` (deps are now inner devDeps; outer root is clean). The old `public/icons.svg` sprite sheet is untouched (unrelated to app icons).
- ✅ 2026-07-03 — Backup nudge at 10 kept cards (Ben: "i dont want a barrier to entry… but if they swipe >9 cards deep i do want to prompt them to make an account to save their brew"):
  - ✅ Trigger: an unlinked (anonymous, no-email) account crossing 10 kept cards (decklist+maybe) DURING a session — growth-gated, so resuming an already-big deck doesn't prompt on entry, only on its next flick. Once per legend per browser (`magicdex-backup-nudge:<legendId>`); "not now" = that brew never nags again; Settings keeps the flow available. Ingest note: Ben confirmed the bulk EDHREC script stays MANUAL — no cron/function wanted.
  - ✅ UI: non-blocking bottom sheet over the swipe (card untouched) — "N cards in — this brew is becoming something…", email input, "save my brew" (updateUser → confirmation link) / "not now". Success panel confirms and closes.
  - ✅ Verified live as an anonymous preview user: no nudge at 9, sheet up past 10 with live count, dismiss sets the flag and further flicks stay quiet, re-armed flag re-prompts. Send path verified to the server: Supabase rejects test domains (`email_address_invalid` — its message renders an empty address, seen live), so both the nudge and Settings now map that to "that email doesn't look deliverable"; a REAL address roundtrip (confirmation link → permanent account) still needs a real inbox — Ben or first users will exercise it. Test legend deleted; lint at the 37 baseline; build clean.
- ✅ 2026-07-03 — Universal-commander relevance LIVE (migrations 014/015 run by Ben; bulk ingest executed with the service key from .env):
  - ✅ `--all` ingest: 3,286/3,321 commanders cached (35 no EDHREC page — battle false-positives + fringe), 856,396 legend_synergy rows, ~55 min wall clock, zero 429 aborts. First attempt died at invasion-of-ikoria (EDHREC 403s non-commander slugs) → fetch now skips ANY 4xx as "no page" (`0c8c289`).
  - ✅ brew_stack v4 verified on never-curated commanders: Fire Lord Azula (added by Ben hours earlier) leads with Storm-Kiln Artist / Electrodominance / Archmage Emeritus — her Spellslinger package; Muldrotha leads with Spore Frog / Kaya's Ghostform; Slimefoot leads with Utopia Mycon / Mycoloth at token-theme boost 4. Hawkeye's curated stack unchanged up top, and her tag tail IMPROVED (theme-derived `burn` joined the pool: Aetherflux Reservoir, Walking Ballista).
  - ✅ 014 purge confirmed; Ben's phone starts clean on a fresh anonymous account.
- ✅ 2026-07-03 — Multi-user VERIFIED end-to-end (Ben enabled anonymous sign-ins + Site URL; every gated flow exercised live in preview as a brand-new anonymous user, then cleaned up):
  - ✅ Invisible sign-in mints an account on load (id ef65bd33… shown in Settings); fresh user sees an empty Box.
  - ✅ Paste-import created legend+deck OWNED by that user (RLS write path); deck-list open healed auto-tags (RAMP 1 / CARD-ADV 1 / DISRUPTION 1).
  - ✅ Chip confirm ladder in the DB: dashed auto DISRUPTION chip → first tap fills solid AND flips deck_card_tags.source to 'user' (read back through the user's own JWT) → second tap removes the row.
  - ✅ Zero-count MASS-DIS cell tapped → "DECKLIST · 0 · MASS-DIS" + "add more" → tag_stack dealt 149 GU sweepers, Cyclonic Rift first — the full veggies flow, under RLS.
  - ✅ Long-press delete removed the test legend; box back to EMPTY; zero console errors across the whole pass.
  - Service-key handling decision: Ben asked if he can share it — answer: never in chat; add `SUPABASE_SERVICE_KEY=…` to the gitignored `mtg-dna/.env` (verified ignored; loadEnv already reads it).
- ✅ 2026-07-03 — Universal-commander relevance (code + migration ready, gated on Ben's five steps) + tag-chip confirm ladder + zero-category taps, from Ben's second-round feedback:
  - **Ben's verdicts:** "only Hawkeye works right… I need this correct for every card in the game that could possibly be a commander" (the generic fallback stack = product death); auto chip tap should CONFIRM (fill in), not remove — remove is the SECOND tap; dots needed an auto/user differentiator (already shipped that way: filled = user, hollow = auto — he couldn't see it through the empty-Box auth blocker); zero-count WREC categories must be tappable; his own data is disposable test data ("my decks are in moxfield") — no claim/backup needed for him.
  - ✅ **`015_brew_stack_v4.sql` (UNRUN)**: legend_tags = Tagger profile ∪ theme-derived otags (top-5 themes through the same alias table), so any commander with an EDHREC page gets tag matches + synergy ordering with zero hand curation. Same signature as v3 (CREATE OR REPLACE).
  - ✅ **`ingest:legend-edhrec -- --all`**: enumerates all commander candidates from the cards cache (legendary creatures + "can be your commander"; filter verified live via PostgREST = 3,321, `*` wildcards required in or() strings), most-played first so interruption still covers real decks, progress ticks, 404-skip counting. DFC/adventure slugs fixed to front-face-only. Dry-runs verified post-refactor (Azula: 278 synergy cards / 135 themes; Hawkeye unchanged). Requires the service_role key (RLS).
  - ✅ **Chip confirm ladder (LIVE)**: tapping an outlined auto chip now CONFIRMS it — deck_card_tags.source flips to 'user' (new `confirmAutoTag`), chip fills solid, dot fills in; the next tap removes. Optimistic with exact-entry revert. (Interactive verification gated on the anonymous-sign-ins toggle — no session is possible in preview until then; build + lint clean at the 37 baseline.)
  - ✅ **Zero-count WREC cells tappable (LIVE)**: a zero IS the gap — tapping filters to the empty view with "add more" right there (the original veggies spec; the inert-zeros call was wrong).
  - ✅ **`014_claim_my_data.sql` REPURPOSED to a purge** of all ownerless pre-013 rows (deck_cards → decks → legends, cascade-ordered).
- ✅ 2026-07-03 — Auth diagnosis + email backup (Ben's report: account row empty, add-legend dead, wipe-on-clear unacceptable):
  - ✅ **Root cause found via direct API probes**: 013 IS run (user_id column exists; RLS live → anon reads return zero rows → the EMPTY Box Ben is seeing) but **Anonymous sign-ins were never enabled** — auth endpoint returns `anonymous_provider_disabled`. No session → RLS rejects every write → add-legend no-oped SILENTLY (my catch swallowed it). His data is intact, NULL-owned, waiting on the claim.
  - ✅ **Errors surface now**: the Settings account row retries sign-in on open and prints the real failure ("Anonymous sign-ins are disabled" — verified in preview); add-legend failures toast the actual message (verified: "new row violates row-level security policy" after fixing upsertLegend's fallback to only trigger on missing-conflict-target errors 42P10/42703 — it had been masking the RLS error with its own).
  - ✅ **Durability (the "lose my fucking mind" fix)**: decks live on the ACCOUNT, and Settings now links an email to it — "back up this box" (`updateUser({email})` → confirmation link → account permanent) and "sign in to my box" (`signInWithOtp`, shouldCreateUser:false so a typo can't mint an empty account — verified: unknown email returns "no box is linked to that email"). One email, no password, used for nothing else; clearing the browser then means sign back in, not data loss.
- ✅ 2026-07-03 — Multi-user auth (code shipped) + deck-list de-clutter (LIVE), from Ben's same-night feedback:
  - **Ben's asks:** RLS + "quick and easy way for users to log in. whatever the lowest friction most private way... i dont want their info i just want them to be able to save their decks" (→ invisible anonymous sign-in); deck list "looks ugly now because theres too much shit"; WREC scores "too busy" for the small screen; header name "smushed by the image" — kill the image, name only, tappable → see the legend.
  - ✅ **Deck-list de-clutter (LIVE, verified at 375px)**: header art REMOVED — commander name only (full width, 44px target), tap opens the full-card overlay (same grammar as the swipe screen's commander bar; unaltered Scryfall image; verified open + dismiss). WREC band rebuilt as five equal grid cells — count over a micro-label, no interpuncts, active filter = amber underline, zeros dimmed/inert (cells measured 70×44 each at mobile width). Collapsed per-row tag TEXT (the noise the auto-tags multiplied) replaced with 6px dots: filled = user tag, hollow = auto (verified: Hawkeye's list shows 6 hollow dots); the expanded chip grid is unchanged.
  - ✅ **Auth code (deployed, inert until 013)**: App.jsx gates first render on a local session check → `signInAnonymously()` when absent (failure swallowed — pre-013 the app is verified unchanged: box loads, settings row reads "not signed in"). New `upsertLegend` one-door in lib/legendDeck.js used by add-legend, paste-import, and the legacy save flow: onConflict "user_id,name" with a legacy "name" fallback so deploy order can't break add-legend (verified live: Niv-Mizzet add landed through the fallback pre-013, then long-press-deleted). SettingsSheet gains an "account" row (short id, tap-to-copy) for the 014 claim.
  - ✅ **Migrations authored:** `013_multi_user.sql` (ownership columns + per-user unique names + full RLS: legends/decks by user_id, deck_cards via deck join, deck_card_tags via two-level join, cache tables world-read + cards authenticated-write for writeBackToCache; brew_stack/tag_stack are security-invoker so they inherit all of this correctly) and `014_claim_my_data.sql` (templated backfill for Ben's user id).
  - **Noticed live during verification:** Ben deleted Captain America, First Avenger and added Fire Lord Azula (created 03:20Z) from his phone — the long-press delete is being used in production. DB assumptions updated: his Box is Team Leader / Hawkeye / Azula.
- ✅ 2026-07-02 — Migration 012 RUN by Ben; theme weighting + gap-fill verified live:
  - ✅ brew_stack v3 verified via direct RPC (Hawkeye, 400 rows): synergy head untouched (End the Festivities still first); the 172-card tag-only tail now leads with 29 `synergy-burn` matches at theme_boost 5 (her #1 EDHREC theme Burn → the burn→synergy-burn alias) — Virtue of Courage, Toralf, Fear of Burning Alive — ahead of every power-matters-only match (Terror of the Peaks, rank 515, correctly below them at boost null). Wrong-context cards now sink within the tail too, exactly Ben's weighting rule.
  - ✅ tag_stack verified via direct RPC: mono-R disruption otags → 400 cards led by Blasphemous Act / Chaos Warp / Vandalblast / Lightning Bolt (rank order).
  - ✅ Gap-fill verified end-to-end in the app on a disposable GU test deck (deleted after; DB confirmed back to the three real legends): deck view → RAMP filter → "add more RAMP" → 400-card ramp stack (Sol Ring, Arcane Signet on top) → ArrowUp flick wrote the deck_cards row + `ramp`/auto tag → full page reload → brew resumed the `wrec:ramp` stack at 398 with the two decked cards excluded. Zero console errors throughout.
- ✅ 2026-07-02 — Ben's follow-up batch (auto-WREC tags LIVE, long-press Box LIVE, theme weighting + gap-fill staged behind migration 012). His verdicts: "why this card" UI dead; theme weighting approved with design delegated; WREC tags weren't auto-populating after swipes (the apply logic had never been built — schema/mapping existed since 009); arrange + delete "should be a long press"; veggies gap-fill flow specified (tap category → filter → add more → category stack, agnostic of plan/EDHREC).
  - ✅ **Auto-WREC tags (LIVE, verified end-to-end on a disposable Tatyova deck, deleted after)**: flick-time — commitCard carries oracle_id; a FRESH deck_cards insert looks up the card's WREC-core otags (card_tags) and writes deck_card_tags rows source 'auto', best-effort so a failure can't double-write the delta (verified: Sol Ring flick → `ramp`/auto row within ~2s). Deck-open heal — loadDeckTags backfills suggestions for rows with ZERO tags (one batched cards query + one card_tags query), then reloads once; covers pre-feature decks and imports (verified: 4 imported bare cards → Cultivate ramp+card-advantage, Counterspell disruption, Rhystic Study card-advantage, Evacuation disruption+mass-disruption, all source auto; WREC band counts them). Trade-off documented: stripping EVERY tag from a card invites its suggestions back next open. The otag→WREC map is imported straight from `scripts/otag-taxonomy.mjs` (one source of truth; `plan` never appears in it, per standing rule — user-only). VISUAL: auto chips render hollow (dashed amber outline, no fill) vs user chips solid-filled; collapsed row tags dim the auto ones (verified via computed styles); tapping PLAN wrote source 'user' and filled solid. moveDeckCard now carries source across merges; toggle-off removes the auto marker; re-add is a user tag.
  - ✅ **Long-press Box slots (LIVE, verified)**: 500ms hold (>10px drift cancels; trailing click swallowed so it never selects) → bottom action sheet (move / delete, backdrop dismisses). MOVE picks the legend up (existing dashed ring) and the next slot tap places AND exits — one gesture arc, not a sticky mode; header shows "cancel" while in flight; the old header "arrange" toggle is deleted. DELETE confirms inline (same copy grammar as the deck view) then deletes outright via the shared deleteLegend + the same localStorage hygiene (order/last-active/brew-session keys). Verified: sheet opens, move swaps Tatyova↔Cap and persists (order re-saved), delete removed the test legend + deck and the Box re-rendered with Ben's three legends untouched; detail pane self-heals via handleLegendsLoaded. Lint dropped to a 37-problem baseline (the removed arrange button took two with it).
  - ✅ **Migration `012_theme_weight_tag_stack.sql` (UNRUN — see Cold Start)**: brew_stack v3 (return type gains theme_boost; top-5 legend_themes matched to otags via exact slug + 13 aliases — extra-combats→extra-combat, tokens→repeatable-token-generator, card-draw→card-advantage, plus-1-plus-1-counters→counters-matter, burn→synergy-burn, … — ordering synergy DESC → theme_boost DESC → edhrec_rank; all 96 stored theme slugs reviewed before writing the alias table) + `tag_stack(p_tags, …)` (category pool, EDHREC-rank ordered, deck rows excluded server-side).
  - ✅ **Gap-fill wiring (LIVE, degrades gracefully until 012)**: deck view → tapping a WREC category filters (shipped earlier) and now ends in an "add more <CATEGORY>" button (plan excluded — the main brew stack IS the plan stack). It deals tag_stack for the category's otags (WREC_TO_OTAGS, same taxonomy import), excludes deck + decided-this-session names, and drops into the swipe. The stack persists/resumes as marker query `wrec:<category>` (never sent to Scryfall; resume + in-place re-sort both handled). Verified: filter narrows ("DECKLIST · 2 · DISRUPTION"), button renders, and pre-012 the missing RPC surfaces as an inline "no cards to deal for this category" (screenshot + network 404 confirmed). fetchBrewStack also now carries theme_boost and sortStack's edhrec key uses it as the synergy tiebreak, so sort round-trips stay lossless post-012.
- ✅ 2026-07-02 — Relevance v2 LIVE: migration 011 run by Ben, EDHREC ingest executed, stack verified end-to-end:
  - ✅ `npm run ingest:legend-edhrec` (real run): Hawkeye 270 synergy cards / 13 themes (Burn 13, Spellslinger 7, Aggro 2); Cap First Avenger 288 / 93 (Equipment 3792, Voltron 811); Cap Team Leader 206 / 26 (+1/+1 Counters 113, Heroes 81). `legend_synergy` = 764 rows.
  - ✅ brew_stack v2 verified via direct RPC call: Hawkeye's top 15 are her EDHREC synergy list in exact score order — End the Festivities 0.684, Firebrand Archer 0.665, Coruscation Mage 0.642, … including **Hawkeye's Bow** (a unique-to-her card, answering "i dont see unique cards to hawkeye"). Altar of Dementia nowhere near the top. Stack grew 354 → 394 (tag matches ∪ EDHREC-page cards).
  - ✅ Preview verified read-only (no flicks): brew on Hawkeye deals End the Festivities on top, Firebrand Archer behind it, "394 IN STACK"; zero console errors.
  - ✅ Sort round-trip fix (found during verification): the RPC's synergy-first order was lost if the user detoured through CMC/A–Z and back — `sortStack`'s "edhrec" key only knew `edhrec_rank`. Now `fetchBrewStack` carries `synergy` on each card and the edhrec sort key is synergy DESC → edhrec_rank ASC (live-search cards have no synergy, so they degrade to plain rank as before). Verified in preview: EDHREC → CMC (Everflowing Chalice/Mox Diamond on top) → EDHREC restores End the Festivities. Lint at the 39 baseline; build 481.61 kB.
- ✅ 2026-07-02 — Stack relevance v2 (code ready, migration 011 UNRUN) + swipe chrome consolidation (SHIPPED, verified in preview), from Ben's same-day stack feedback:
  - **Ben's verdicts:** shared otags match the wrong CONTEXT (Hawkeye carries power-matters because she pumps HERSELF — a burn commander — yet Altar of Dementia led her stack); EDHREC's per-commander top tags are the "main theme" signal; his EDHREC high-synergy cards weren't on top; swipe chrome still messy — wants commander shown above the stack (tappable → re-read the card), a "done" → deck list, home bottom-right, search bottom-left.
  - ✅ **Migration `011_legend_edhrec.sql` (UNRUN)**: `legend_synergy` (per-legend EDHREC cardlist rows: name_lower join key, synergy/num_decks/potential_decks/source_list) + `legend_themes` (rank-ordered taglinks — Hawkeye: Burn 13, Spellslinger 7; stored for display + future theme→otag weighting, not yet consumed) + brew_stack v2 (DROP+CREATE, return type gains `synergy`): pool = tag matches ∪ EDHREC-page cards, ordered synergy DESC nulls last → edhrec_rank. High-synergy cards lead; wrong-context tag matches sink to the tail.
  - ✅ **`scripts/ingest-legend-edhrec.mjs`** (`npm run ingest:legend-edhrec`): per legend — name→slug (verified: hawkeye-young-avenger), fetch json.edhrec.com commander page, all container cardlists deduped by name + taglinks → upsert + prune. Dry-run verified: Hawkeye → 270 synergy cards, 13 themes. Payload structure live-verified before building (cardviews carry name/synergy/inclusion/num_decks; themes at panels.taglinks).
  - ✅ **Swipe chrome (SHIPPED)**: ONE commander bar on top — tappable commander anchor (34px art + name + "N in stack"/reconnecting subline; tap → full-card overlay, cache-first fetch, tap-anywhere dismiss, unaltered image) + UNDO (when history) + SORT chip + "done (N·N)" in primary color → deck list. The old two-row header ("review (N·N)" + "N IN STACK · NAME" strip) is gone. Bottom: SEARCH bottom-LEFT, HOME glyph bottom-RIGHT (exits session; replaces back arrow), gesture hint center. Card geometry untouched (topReserve/bottomReserve unchanged — the card is sacred). Verified in preview: bar/overlay/dismiss/buttons all live; build passes (481.55 kB); lint at the 39 baseline.
  - ✅ (resolved same day) Ben ran 011; ingest + verification in the block above.
- ✅ 2026-07-02 — Brew stack wired: Box → brew deals the legend-relevant stack (migration 010 APPLIED + RPC live + app wiring, verified in preview):
  - ✅ Migration `010_brew_stack.sql` run by Ben; `ingest:cards` re-run populated `legal_commander` on 31,622 cards (oracle bulk includes un-sets/banned/Alchemy — the function filters them out).
  - ✅ RPC verified live: Hawkeye → 354 commander-legal mono-R cards, EDHREC-ordered, each with `matched_tags` (top: Chandra's Ignition #480 power-matters/scales-with-power; Solphim synergy-burn; Torbran damage-increaser).
  - ✅ `fetchBrewStack` in scryfall.js (one door: legend name → cached oracle_id → RPC → cache-shaped cards with `matched_tags` + `edhrec_rank`; returns [] on any failure so callers can fall back).
  - ✅ Brew.jsx: `fetchDefaultStack` = RPC-first, generic live seed as fallback (unprofiled legend never strands). Used by BOTH the initial seed and session resume (a resumed default seed goes back through the RPC — persisted `query` of ""/"-t:land" detected via `isDefaultSeedQuery`). Typed searches stay on live Scryfall untouched. `handleSortChange` re-sorts an RPC stack in place (cards carry edhrec_rank, so all three sorts work client-side) instead of re-running the generic search, which would have silently swapped the relevance stack for an unfiltered one. Deck/decided exclusions unchanged (client-side, recomputed per entry).
  - ✅ Verified in browser preview against the live DB, read-only (no flicks — Hawkeye's deck is Ben's real data): swipe shows "354 IN STACK · HAWKEYE, YOUNG AVENGER", EDHREC label, Chandra's Ignition on top; network shows ONE `rpc/brew_stack` POST and NO generic live-seed search call. Build passes (480.92 kB); lint exactly the 39 pre-existing problems.
  - ⚠️ `matched_tags` rides on every stack card but is NOT yet shown in the UI — design the presentation with Ben (it's also the otag→WREC auto-tagging input).
- ✅ 2026-07-02 — Migration 009 RUN + all three ingests executed against the live DB (Ben ran the migration; ingests via the new sb_publishable key — open-access posture means anon tier writes pass):
  - ✅ `ingest:cards` re-run: 38,233 rows upserted, `edhrec_rank` populated on 31,675 (rest genuinely unranked on Scryfall; Krenko spot-check = 1100).
  - ✅ `ingest:tags`: 49,144 rows across all 35 taxonomy tags, zero 429s, every live count matched the taxonomy's recorded probe counts.
  - ✅ `ingest:legend-tags`: all three legends profiled from their real Tagger pages — Hawkeye, Young Avenger [msh/131] 5 tags (power-matters family + synergy-burn); Captain America, First Avenger [mar/88] 20 tags (equipment + burn/removal families + mana-sink); Captain America, Team Leader [msc/5] 11 tags (typal-hero + pp-counters family). Six 429s absorbed by the escalating backoff (60s Retry-After honored each time). `card_tags` final: ~74k rows, 36 profile rows source `tagger-card-page`.
  - ✅ Two live-run bugs found + fixed (both committed): (1) flavor-catalog tags leaked through Cap's page — `alliteration` (a 4,169-card tag of alliteratively-NAMED cards), `card-names`, `personal-text`, `face-commander` → added to TAG_BLOCKLIST, their ~2,375 pool rows deleted from card_tags, legends re-profiled clean; (2) profile rows lost provenance — a legend sits inside its own tags' pools, so the pool upsert flipped the shared (oracle_id, tag) row's source back to 'otag-search' → profile write moved AFTER pool pulls (last write wins), existing 36 rows' source repaired via PATCH.
  - ⚠️ **Design input for the stack query (discovered live): PostgREST cannot embed across card_tags→cards (no FK, deliberate per 009), and `in()` with 1,000+ oracle_ids exceeds URL limits — the app-side stack query needs an RPC (Postgres function, migration 010) or a join view.**
- ✅ 2026-07-02 — Brew-relevance data foundation (journey statement received; verified against live endpoints before building, per the never-guess-API-formats rule):
  - ✅ **Ben's complete user journey (canonical)**: late-night Tinder-style card swiping + his real deck-building process. Select a legend in the magıcdex Box (Pokémon-storage-box nostalgia) → brew = swipe a stack FILTERED to that legend's Scryfall Tagger tags (token legend → token synergy; burn legend → burn spells), ordered by EDHREC ranking by default → big pile of liked cards → WREC tagging (auto-populated + visually distinct from manual, EXCEPT `plan` which is always user's) → deck list. Must stay snappy: nightly-ish data pulls, manual refresh for spoiler season. "Scryfall Tagger"/"EDHREC" always mean the per-card pages as structured data sources.
  - ✅ **Live verification (2026-07-02)**: official `otag:` search indexes Tagger's community tags (removal 6,367 / burn 2,989 / ramp 2,243) and ROLLS UP the tag hierarchy; `edhrec_rank` ships on every Scryfall card object; `json.edhrec.com/pages/commanders/<slug>.json` is the commander page as clean JSON (unofficial); tagger.scryfall.com per-card GraphQL works with just page-cookie + CSRF meta (verified query shape in ingest-legend-tags.mjs; unofficial); api.scryfall.com 400s without BOTH User-Agent and Accept headers; sustained pagination gets 429'd well below the documented 10/sec (observed after ~25 pages at 120ms — hence 250ms + escalating backoff honoring Retry-After).
  - ✅ **Migration `009_card_tags.sql` (UNRUN — see Cold Start)**: `cards.edhrec_rank` + index; `card_tags` (oracle_id, tag, source ∈ otag-search|tagger-card-page, PK (oracle_id,tag), deliberately NO FK to cards for spoiler-season ingest-order independence); `deck_card_tags.source` ∈ user|auto default user.
  - ✅ **`scripts/otag-taxonomy.mjs`**: 35 verified otag slugs (11 WREC-core with otag→WREC mapping — `plan` never appears by rule — + 24 theme tags), each with live-probed card counts; alias notes (sweeper=boardwipe=mass-removal, blink=flicker, typal=tribal); TAG_BLOCKLIST for catalog noise (cycle/cycle-*, filterout*, bare activated/triggered-ability). Of ~105 probed candidates 81 were misses (tokens = `repeatable-token-generator`, nothing guessable) — never add a slug without a live probe.
  - ✅ **`scripts/ingest-tags.mjs`** (`npm run ingest:tags`): full paginated pull per taxonomy tag → card_tags upsert + stale-row prune; `--dry-run`, `--tag=X`. Dry-run verified (sweeper: 925 ids over 6 pages).
  - ✅ **`scripts/ingest-legend-tags.mjs`** (`npm run ingest:legend-tags`): per Box legend — printing lookup (official API) → Tagger page GraphQL profile (ORACLE_CARD_TAG + ancestors, blocklist-filtered) → card_tags rows source 'tagger-card-page' → for legend tags outside the taxonomy, top-6-pages EDHREC-ordered otag pull (~1,050 most-played cards per tag). Dry-run verified on Krenko (7 tags incl. repeatable-creature-tokens; 429 backoff exercised live and recovered). `--dry-run --name="..."`.
  - ✅ **`scripts/ingest-shared.mjs`**: one copy of env loading + polite fetch (250ms spacing, UA+Accept, escalating 429 backoff) + paginated otag walker + card_tags writer/pruner, so the tag scripts can't drift on compliance.
  - ✅ `ingest-cards.mjs` now maps `edhrec_rank`; package.json gains both ingest scripts. Lint clean on all scripts.
  - **Held for design conversation (not built)**: per-commander EDHREC synergy cache (Ben approved using the unofficial JSON feed; table shape TBD → migration 010), the stack query + swipe wiring, auto-tag apply logic + visual treatment, Vercel Cron for nightly runs (scripts are manual dev-machine, house pattern).
- ✅ 2026-07-02 — Device-pass fix batch (5 commits, from Ben's on-device feedback):
  - ✅ `c19dff7` Theme: dark is the DEFAULT (light = explicit settings choice) — following the system's light preference made first load flash light before the always-dark brew took over; system-preference listener removed.
  - ✅ `29ddbac` Detail pane cache-first — LegendIdentity's sprite fetched via LIVE api.scryfall.com on every legend select (the slowest thing on Home); now getCardData (memo + local cards cache). Verified: zero live named calls on select.
  - ✅ `b444583` Delete is OUTRIGHT — per Ben: the legend leaves the box with its deck (legend row + deck + cards + tags, child→parent). `deleteLegend(legendId, deckId)` replaces deleteLegendDeck; saved box order + last-active keys cleaned. Verified live on a disposable legend.
  - ✅ `410d8a5` Deck view: uniform 3×2 action grid — five WREC chips + move-to-board as a sixth equal cell (no hanging PLAN, move is "a button like the rest" per Ben); delete row reads "delete legend" with matching confirm copy.
  - ✅ `6f747c5` Add-legend search results identifiable — art thumbnail + set name per row (six same-named Hawkeyes were indistinguishable). Plus an iOS keyboard viewport pin (scrollTo(0,0) after keyboard settles) for the status-bar bleed — **needs on-device confirmation**, the pan can't repro in desktop preview.
  - **NOTE: Ben brewed a real Hawkeye, Young Avenger deck on his phone 2026-07-02 (6 cards + maybeboard) — live data now includes it. Disposable-test-data assumptions must account for it.**
- ✅ 2026-07-02 — Deck view: WREC band filters the list on tap (`31aa0fb`):
  - ✅ Non-zero categories in the frozen WREC band are tappable filter toggles: tap RAMP → both sections narrow to ramp-tagged cards; tap again → clear. One category at a time. Active = amber + underline; zero categories stay dimmed readouts. Section headers follow the filtered view ("DECKLIST · 2 · RAMP").
  - ✅ Band height unchanged — segment tap targets reach ~44px via vertical padding + negative margin, so the frozen header doesn't eat list space.
  - ✅ Verified live on a disposable imported test deck (filter, switch, clear, zero-category inertness), test data cleaned up after. Build passes; lint unchanged (39 pre-existing).
  - **The roadmap's Active tier is now CLEAR** — remaining work is the design-session tier (chip form, soft corners, Moxfield-style categories, WREC-as-search-taxonomy) and B.I.L.L. architecture, all of which need conversation with Ben first.
- ✅ 2026-07-02 — Box arrange mode: manual slot reordering (`69a77c7`):
  - ✅ Box header gains an "arrange" toggle (right side, mono, ≥44px, shows only with 2+ legends) — explicit mode per the Pokémon box Move mode, no hidden long-press. Arranging: tap to pick up (dashed amber ring, distinct from the solid selection ring), tap another slot to swap, "done" to exit. Detail-pane selection untouched in-mode; add tile dimmed/inert; pager chevrons stay live so swaps cross boxes.
  - ✅ Order persists as an id array in `localStorage["magicdex-box-order"]` — the codebase's established localStorage-now-column-later pattern (same as `magicdex-last-legend`; a `legends.position` column can back it when multi-device matters, deliberately NOT an unrunnable-until-manual migration now). Display = saved ids first, then unlisted legends alphabetical (new adds land after the arranged block); no saved order = exactly the old alphabetical behavior.
  - ✅ Verified in the browser preview: swap flips slot order and survives reload; normal tap still selects; cleared order falls back alphabetical. Zero DB writes. Build passes; lint unchanged at 39 pre-existing problems.
- ✅ 2026-07-02 — Deck view: move a card directly between mainboard ↔ maybeboard (`b3f0067`):
  - ✅ Each expanded deck-view row (below the WREC chips) gains "move to maybeboard" / "move to mainboard" — the direct path that used to require remove + re-swipe. Moves ALL copies.
  - ✅ Write is `moveDeckCard` in `lib/deckTags.js`: a section UPDATE on the existing deck_cards row (tags reference the row id, so they ride along) — deliberately NOT a −qty/+qty through the flick-write queue, whose delete-emptied-row behavior would cascade the WREC tags away. Same-card-on-both-boards merges: tag union onto target, quantity sum, source row dropped.
  - ✅ Brew.jsx handler mirrors the tagging pattern: optimistic instance/tag-entry hop, exact prior state restored on failure, tags re-read after success.
  - ✅ Verified live on a disposable imported test deck, all three paths DB-confirmed: simple move kept the tag; reverse move; merge produced qty 2+1=3 with unioned tags (plan+ramp). Test data cleaned up — DB back to exactly the two Captain America legends / 23 deck_cards. Build passes; lint 39 problems (down from 43, none in touched files).
- ✅ 2026-07-02 — Delete a deck: confirmed destructive row in the deck view (`76ab870`):
  - ✅ Deck view (ReviewScreen, live sessions) gains a dimmed "delete deck" row at the END of the list — separated from export (top-right) and back/home (bottom nav) per NN/g destructive-action placement. Tap → inline confirm expands in place (no modal): copy states what's removed (N cards + tags) and what survives ("<legend> stays in the box at 0/100"), then 44px cancel / delete (danger outline). Failure surfaces inline and re-enables; success exits to the Box.
  - ✅ One door for the delete: `deleteLegendDeck(deckId)` in `lib/legendDeck.js` — deck_cards first (deck_card_tags cascade at the schema level per 006), then the decks row (deck_cards→decks has NO cascade, order is load-bearing). Legend row never touched.
  - ✅ Brew.jsx handler: drops pending flick-writes (queued +1 can't race the delete), clears the persisted `magicdex-brew-session:<legendId>` (next brew seeds fresh), resetBrew + onSessionDone → Box reloads, legend gated 0/100 "no deck yet".
  - ✅ Verified live end-to-end in the browser preview on a disposable test deck (imported Loki + 3 cards via the paste flow, exercised cancel then delete, confirmed deck_cards back to exactly the pre-test 23 rows and the Box showing Loki 0/100). Test legend removed after — DB restored to exactly the two Captain America legends. Build passes; lint at the same 43 pre-existing problems.
- ✅ 2026-07-02 — Load-lag root cause found + fixed: batched per-card cache lookups (`7d205b9`):
  - ✅ Root cause (the persistent "load lag" complaint): the deck view (ReviewScreen) and paste-import (`resolveImportLines`) resolved card data one name at a time through `fetchCardIdentity` — one Supabase round-trip PLUS one CORS preflight per card, squeezed through the browser's ~6-connections-per-host cap. Measured live: opening the 23-card Team Leader review fired 46 requests (23 GETs + 23 OPTIONS); a 100-card paste-import awaited ~100 lookups strictly serially (~8s+ fully cached).
  - ✅ Fix: new `getCardDataBatch(names)` in `src/lib/scryfall.js` — one chunked `in(name_lower, …)` query (100 names/chunk) against the `cards` cache, feeding the same memo `getCardData` uses; true cache misses come back separately and only they walk the throttled live exact→fuzzy path. ReviewScreen paints all hits in ONE state merge (was one re-render per card); import batches first, then resolves only unresolved names live.
  - ✅ Verified against the live DB in the browser preview: review now issues ONE cards query; a paste-import with a deliberately fake name issued one batch query and only the fake card touched api.scryfall.com (exact 404 → fuzzy 404 → flagged unresolved, import continued). Names with commas/apostrophes/slashes (King T'Challa // Black Panther, SP//DR) survive supabase-js `in()` quoting. Build passes; lint shows only pre-existing errors.
  - Not batched (deliberately, small-N): LegendBox's lazy identity heal (runs per newly-added legend, usually 1) and single-card lookups (commander detail, add-legend) — all still cache-first singles.
- ✅ 2026-07-01 — Fix empty-state render crash after DB wipe (`80e5b27`):
  - ✅ Root cause: `008_one_deck_per_legend.sql`'s `decks_legend_id_unique` constraint (applied 2026-06-27) made PostgREST infer `legends -> decks` as to-one, so the `decks(...)` embed now returns a single object/`null` instead of an array. `resolveLegendDeck` (lib/legendDeck.js) and a duplicate inline calc in LegendBox.jsx still did `(decks ?? []).reduce(...)`, assuming an array — threw `TypeError: (intermediate value).reduce is not a function` on every legend once the DB was wiped (previously masked because pre-wipe legends still had array-shaped decks from before the constraint).
  - ✅ Fix: `resolveLegendDeck` now normalizes any shape (array, single object, or null) internally via a `toDeckArray` helper, so every caller — LegendBox, LegendIdentity, `fetchLegendDeck` — is safe regardless of what PostgREST returns. LegendBox's duplicate `deckTotal`/reduce logic deleted in favor of the shared `deckTotal`/`resolveLegendDeck` from `lib/legendDeck.js`.
  - ✅ Verified live against the actual wiped Supabase project via dev server + browser preview: empty Box (no legends) and a legend with zero decks both render (0/100, "no deck yet") with no crash; a legend with an existing deck (Captain America, Team Leader, 24/100) still resolves and displays correctly. Build + lint pass (only pre-existing, unrelated warnings).
  - ✅ Swept the rest of the codebase for the same embed-shape assumption — no other file reads `.decks` off a legend row; `deck.deck_cards` (deck -> deck_cards) is unaffected since that relationship has no unique constraint and stays to-many.
  - Added `.claude/launch.json` (mtg-dna-dev, `npm --prefix mtg-dna run dev`) so the Preview tool can launch the dev server for future verification passes.
- ✅ 2026-06-27 — Clean-slate wipe + migration 008 applied: all `legends` (9), `decks` (14), `deck_cards` (494), `deck_card_tags` (114) rows deleted via the anon REST API (PostgREST, FK-safe order child→parent) — disposable test data, canonical decklists live in Moxfield. `cards` Scryfall cache (38,178 rows) confirmed untouched throughout. With `decks` empty, `008_one_deck_per_legend.sql` (`decks_legend_id_unique` UNIQUE constraint on `decks.legend_id`) applied cleanly in the Supabase SQL editor — no per-legend duplicate reconciliation needed. Migration file's header comment updated to reflect APPLIED state.
- ✅ 2026-06-19 — Brew nav + review polish (3 commits):
  - ✅ `9af0692` Brew nav: discoverable review entry, back+home exits on review — swipe screen's tally is now a labeled "review (N·N)" control (checklist icon + text, was a bare underlined count); ReviewScreen gained explicit bottom thumb-zone BACK (→ swipe, resumes at `swipeIndex`) and HOME (→ resetBrew + onSessionDone, exits to legend) controls for live sessions, replacing the ambiguous top-left chevron there (Brew.jsx suppresses it when `brewView === "review" && session`). Both new exits are direct state/prop calls, independent of the existing hardware-back history-trap ladder — chosen so BACK/HOME always target the same place regardless of `reviewOrigin`, unlike the chevron.
  - ✅ `4e4d7d8` Review: actually freeze commander anchor + WREC band on scroll — root-caused the earlier "sticky" commit (`c2edec6`, 2026-06-16) never actually freezing anything: ReviewScreen's own root div had `overflowY:auto` *and* only `minHeight` (no capped height), so it became the nearest CSS scroll container for its sticky child but never overflowed itself (always grows to fit content) — the sticky header had no clipped viewport to pin against and scrolled away with the page. Fix: removed `overflowY:auto` from that div so Brew.jsx's fixed `inset:0` takeover wrapper is the sole scrolling ancestor; sticky now resolves against it correctly. Verify by scrolling a 63-card list to the bottom — anchor + WREC band stay visible (code-verified only, not yet checked on device).
  - ✅ `4addad4` WREC chips: soft 6px corners (scoped exception) — the five RAMP/CARD-ADV/DISRUPTION/MASS-DIS/PLAN tag chips get `borderRadius: 6` for a pill feel; deliberate, scoped exception to the app's no-radius rule, called out in a comment so it isn't "fixed" back to 0 later. No other chrome touched.
  - ⚠️ All three are code/build-verified only (lint: same 47 pre-existing problems as `main`, none new; build passes, same bundle hash as the unsplit working version). Not exercised in a browser — review the discoverability/nav changes and the sticky fix on a live device or dev server before considering this UX-verified.
- ✅ 2026-06-18 — Ran `npm run ingest:cards` manually (service key supplied inline, not saved to `.env`): streamed Scryfall's `oracle_cards` bulk file (~168 MB), upserted 38,178 rows into the `cards` table (0 skipped, 77 batches). Confirms `scripts/ingest-cards.mjs` (added `e41eeff`) works end-to-end against the live Supabase project. `SUPABASE_SERVICE_KEY` is still not in local `.env` — re-running later requires supplying it again.
- ✅ 2026-06-16 — Brew/deck-view session (5 commits):
  - ✅ `85329a5` Deck view: type/mana/oracle context on review rows. Each row shows a dimmed Noto Sans Mono subline — type line (subtypes per the LegendIdentity display rule) + mana cost; expanding a row for tagging also shows oracle text (DFC-aware, both faces). deck_cards stores only name+quantity, so data is fetched through a NEW single helper `getCardData(name)` in scryfall.js — memoized + serialized behind a 100ms politeness delay, structured as the seam the future Supabase bulk cache slots behind (DATA_SOURCES.md). Unresolvable names render name + dimmed "card data unavailable", never a blank row.
  - ✅ `c2edec6` Deck view: commander anchor + WREC band now BOTH sticky (one frozen header block, spreadsheet top-row behavior) — they stay visible as the list scrolls; safe-area top inset respected; content clears the header at rest so the first row isn't hidden. WREC band moved out of the scrolling content into the sticky wrapper.
  - ✅ `3093021` Brew search: invalid Scryfall syntax (400/422 + `details`) no longer fails silently. New `invalidQueryError` tag in scryfall.js distinguishes bad-syntax from valid-but-empty; `runSearch` returns an outcome `{ok,kind,message}`. Inline swipe search now awaits it and shows a brief dimmed toast ("invalid search" / echo Scryfall details if short / "no cards found"), keeping the field open to fix. AddLegendSheet gets an `onInvalid` toast (empty stays a silent empty list — normal while typing).
  - ✅ `1b6b1d5` DFC flip control: single dimmed-mono "flip" label (≥44px, aria-label) toggling either direction, replacing face-specific FRONT/BACK.
  - ✅ `47622bb` Brew per-legend session resume: transient swipe-session view state (active seed/query + queue index + lastActive) persisted to `localStorage["magicdex-brew-session:<legendId>"]`. Reopening a legend rebuilds the exact session (same seed/search, same clamped position); decided cards still excluded via existing dedupe. 30-day TTL — stale sessions discarded on read (key cleared) and fall back to a fresh default seed; a persisted seed that now errors/returns nothing also falls back gracefully. The DECK is never touched by expiry (it persists via flick-is-a-write to deck_cards). Helpers `loadBrewSession`/`saveBrewSession` + `resumeOrSeedSwipeQueue`/`tryResumeSwipeQueue` in Brew.jsx.
  - ✅ Build passes (462.11 kB). Lint: no new errors introduced (pre-existing scryfall/Brew/SwipeScreen warnings unchanged).
  - ⚠️ Task 1 still hits live Scryfall per card (throttled/memoized) — exactly the per-card read the not-yet-built bulk cache will serve (DATA_SOURCES.md open item). All deck-view tagging context UNVERIFIED on a live device.
- ✅ 2026-06-13 — Data-source compliance: DATA_SOURCES.md + Box-gate scrim (2 commits):
  - ✅ `74ca4ef` New DATA_SOURCES.md (sixth brain doc): Scryfall/EDHREC rate limits, local-cache-first target architecture, image terms (no desaturation/distortion/cropping attribution), product rules (stay free). Read before any data-source/external-API work.
  - ✅ `7a21e41` Box gate scrim: removed the `grayscale(1) brightness(...)` CSS filter from gated LegendBox slot art (Scryfall image-terms violation) and replaced it with a dark scrim overlay (rgba(10,14,26,0.55)) over UNALTERED art; N/100 badge + name stay legible above it; 100 = clean art. LegendIdentity hero verified already filter-free (left unscrimmed — detail must show the card; lock shown via DECK N/100 readout). Swept: only other image filter is a `blur` in legacy/unreferenced PileSwipeScreen.jsx (flagged, out of scope).
  - ⚠️ Open compliance items remain in DATA_SOURCES.md: art_crop artist/copyright attribution verification, and the future bulk-data→Supabase cache architecture (not yet built — app still hits live Scryfall per-request).
- ✅ 2026-06-13 — WREC multi-tag categorization (4 commits):
  - ✅ `97638bc` Schema + lib: migration `006_wrec_tags.sql` (UNRUN, see Cold Start) — `wrec_tag` enum (ramp/card-advantage/disruption/mass-disruption/plan, rejects others) + `deck_card_tags` join table (deck_card_id → deck_cards on delete cascade, unique per (deck_card_id, tag)). `src/lib/deckTags.js`: tagCard/untagCard (immediate writes, idempotent) + fetchDeckCardsWithTags(deckId) → cards with `tags: string[]`. This is the input that feeds WREC / the long-broken _deckCategory.
  - ✅ `c2e0648` Review-time tagging UI (swipe untouched/binary): each live deck-view row is tap-to-expand → five 44px multi-select chips (RAMP/CARD-ADV/DISRUPTION/MASS-DIS/PLAN), active=amber fill, inactive=dim outline, sharp corners, mono. Immediate writes. Collapsed rows show active tags compactly; untagged rows show nothing (no "uncategorized"). Brew owns tag state (loads on review open, optimistic handleToggleTag) so ReviewScreen stays db-free. × remove bumped to 44px.
  - ✅ `4a2120a` WREC count summary band: one mono line below the commander anchor — five categories + counts (by quantity), zero categories dimmed as the dump-stat tell. Live-updates with chip toggles. Composition view, sums can exceed deck size (multi-tag), not normalized.
  - ⚠️ All UNVERIFIED on live data until 006 is run in Supabase (tags read/write will error otherwise — app still renders).
- ✅ 2026-06-13 — UX_AUDIT.md + bottom-controls NN/g pass (2 commits):
  - ✅ `3532a8f` New UX_AUDIT.md (fifth brain doc): NN/g mobile standing constraints + per-session audit status. Read before any UX work.
  - ✅ `3611dfc` Bottom-control redesign + NN/g pass. SwipeScreen: BACK→bottom-left, SEARCH→bottom-right flanking the gesture hint; search expands an inline field (type=search, clear ×, cancel, Enter-submit, autocaps/correct off, pre-filled with active query) over the hint but not the back button; top-header back chevron removed (legend name + tally kept). Touch-target sweep to ≥44px: bottom controls, flip button, UNDO/SORT, sort-menu rows, box pager chevrons (was 36×28), Home settings gear (was 40×40), settings segmented sort, add-legend input + new close ×. Overlay dismiss: AddLegendSheet + SettingsSheet got visible close ×'s (backdrop tap still closes). Brew wires `query` + `onInlineSearch` (→ runSearch) to SwipeScreen. UX_AUDIT status updated (reachability DONE, touch targets SWEPT, overlays PASS, inputs PASS; swipe-ambiguity left UNVERIFIED/device-only). Build 454.41 kB.
  - ⚠️ Device-unverified: the inline-search expand/cancel, the bottom thumb-zone reach, and the 44px hit areas were code-verified only — needs an on-device pass (swipe-edge ambiguity also still UNVERIFIED per the audit).
- ✅ 2026-06-13 — Game Changer: add "GAME CHANGER" text tag alongside glow (`3b7341c`):
  - ✅ Kept the amber breathing glow as the ambient flag; added a small "GAME CHANGER" tag (Noto Sans Mono, uppercase ~10px, amber on a sharp dark plate) at the card's bottom-left, padded clear of the card corner + the bottom-right flip button. Shows only when isGameChanger; detection unchanged. Build 451.54 kB.
- ✅ 2026-06-13 — Brew defaults + copy/indicator tweaks (3 commits):
  - ✅ `8bde639` duplicate-add toast copy: "already in your box" → "already in the dex".
  - ✅ `2cdb748` Game Changer glow-only: removed the Material Symbols bolt glyph + dark plate; the amber breathing corner glow is now the sole indicator. Detection unchanged.
  - ✅ `326fcb2` Brew defaults: new `src/lib/brewDefaults.js` (get/set over `magicdex-brew-defaults`, object-structured for future prefs; defaults sort=edhrec, excludeLands=true, with commented placeholders for budget/UB/printing/scryfall-syntax/ARBITER-ORACLE). SettingsSheet "brew defaults" subsection: segmented default sort (EDHREC | A–Z | CMC ↑) + exclude-lands ON/OFF, persisted immediately. seedSwipeQueue reads defaults (chosen sort → initial swipeOrder + Scryfall order; -t:land only when excludeLands on). handleSortChange re-runs for any session even with empty query (lands-included seeds have no -t:land term). Per-session controls still override. Build 451.20 kB.
- ✅ 2026-06-13 — Home detail: fixed layout, deck row always reserves space (`69c10f1`):
  - ✅ Root cause: the deck row rendered conditionally (`{primary && …}`), so a deck-less legend had a shorter footer and the flex row gave the card sprite + readout extra height — they rendered larger for a fresh legend than for one with a deck.
  - ✅ Deck row is now ALWAYS rendered (constant footer height); no deck → dimmed "no deck yet" + "0/100" in the identical footprint, non-interactive (dimmed chevron). Card footprint + text sizing are identical at 0 cards or 99.
  - ✅ Build passes (450.10 kB)
- ✅ 2026-06-13 — Detail/swipe/add polish pass (5 commits):
  - ✅ `8828c3a` Home detail: fixed card preview footprint — the detail-pane card box now has a fixed footprint (height 100% of pane, width 63:88) with the `<img>` absolutely positioned so the source image's intrinsic size can't drive it; every legend fills the identical box (fixed the Atraxa-vs-Hawkeye height mismatch).
  - ✅ `14c7246` Swipe sort label reflects active order — seedSwipeQueue sets swipeOrder="edhrec"/dir="asc" so the control reads "EDHREC" on a fresh seed instead of the lying "NAME"; NAME/CMC overrides change label+order together; EDHREC label drops the asc/desc arrow.
  - ✅ `51ded8d` Home detail type = subtypes only — strips the leading "Legendary Creature — " (and "Legendary Artifact Creature — " etc.) display-only, showing just the subtypes; falls back to the full type for non-creature commanders. Raw type stays in data.
  - ✅ `29fcf5d` Add legend dedup — handleAddLegend matches on scryfall id (never name); duplicate → don't insert, select the existing legend (last-active) + flash a dimmed "already in your box" toast; new inserts now store scryfall_id immediately so the guard works before the lazy heal.
  - ✅ `a6d3a80` Game Changer indicator restyle — detection unchanged; the cyan/purple electric glow + blue SVG bolt became a single-hue amber breathing glow (hugging the card corners) + a Material Symbols "bolt" amber-on-dark sharp-edged chip on the card's top-left corner.
  - ✅ Build passes (450.05 kB)
- ✅ 2026-06-13 — magicdex Home: single fixed Pokémon-box screen, paged slots, no scroll (`0912759`):
  - ✅ Home is now ONE fixed screen, no scrolling anywhere (root pads all four safe-area insets; proportional flex). Wordmark bar (~6vh: magıcdex left + gear right) → detail pane (flex 46) → box tray (flex 42).
  - ✅ LegendIdentity reworked from the art_crop/oracle block into a device readout: LEFT the actual Scryfall "normal" card image (corner-masked, sized to pane height — the "sprite"), RIGHT label/value fields (mono labels, Zilla Slab values: name/type/mana/status/deck N-100), footer = tappable deck row + brew button. Empty box → "BOX EMPTY" plate.
  - ✅ LegendBox is now a paged fixed grid: header pager (‹ BOX N › chevrons, disabled at ends), a fixed 4×2 = 8-slot grid filling the tray (1fr/1fr, 6px gaps, never scrolls). Filled slot = art_crop + name band + 100-gate grayscale/N-100; selected slot gets a 2px amber cursor border. Add tile sits after the last legend; rest are empty inset cells. Legends beyond 8 page to box 2,3… via chevrons; current box persisted (`magicdex-box`, clamped on reload).
  - ✅ Tap filled slot → updates detail pane in place + sets last-active, no scroll/no push.
  - ⚠️ UNVERIFIED on device: the no-scroll/no-clip guarantee and the `height:100%`+`aspect-ratio` card sprite sizing in flex were only built + desktop-built, not run on a real iPhone SE/tall phone. Needs a device pass (see Cold Start).
  - ✅ Build passes (449.07 kB)
- ✅ 2026-06-12 — magicdex Home: two-pane storage-box layout, uniform slots (`49488a6`):
  - ✅ Home is now a Pokémon-box two-pane layout (flex column): top bar (magıcdex wordmark dropped to eyebrow position + settings gear) → TOP PANE (~55%, flex 55) selected legend's embedded LegendIdentity, scrolls internally, dim "add your first legend" when no legends → BOTTOM PANE (~45%, flex 45) the box tray on a distinct surface (paper/surface bg + top border).
  - ✅ LegendBox reworked from a 3-col 5:4 grid + separate centered empty state into the tray: "BOX" + count header bar, then a 4-col uniform square-slot grid (8px gaps) that scrolls internally. Filled slot = legend art_crop cropped to fill, name band, gate grayscale + N/100 badge; selected slot gets an overlaid amber/gold ring (`activeId` prop). Add tile = first empty slot; then non-interactive empty slots pad the current row + one extra row (subtle dashed border) for visible room to grow.
  - ✅ Tapping a filled slot swaps the top detail pane (existing last-active behavior), no push.
  - ✅ Build passes (447.87 kB)
- ✅ 2026-06-12 — Deck view: commander anchor header, remove vestigial pile section (`5f3991c`):
  - ✅ Live legend sessions now show a sticky commander anchor in ReviewScreen instead of "DECK · N CARDS": legend art_crop (56px, corner-masked 5.5%/4% per the swipe card) + name (Zilla Slab) + "N/100" (Noto Sans Mono). Count uses the app-wide deckTotal convention (all board instances + commander) so it matches the Box tile / identity deck-row badge. Anchor padded left of the brew back chevron so they don't overlap. Brew passes `commander={{ name, art: image_uri }}`.
  - ✅ Pile section deleted — vestige from the pre-carousel iteration, never populated. ReviewScreen no longer takes/renders `pile`; Brew stopped passing it. View is now: anchor → DECKLIST → MAYBEBOARD (only when non-empty).
  - ✅ Maybeboard verified: renders its own section when non-empty, omitted entirely (no "—") when empty.
  - ✅ Non-session save flow (no legend) keeps the text header + name inputs.
  - ✅ Build passes (447.21 kB)
- ✅ 2026-06-12 — magicdex shell restructure: Box-as-root, no tabs (4 commits):
  - ✅ `1ac4843` Box-as-root Pichu layout, tab bar removed — App renders ONE scrolling surface (Home) with Brew as a fixed takeover; NavBar unmounted. Home top block = last-active legend's identity (LegendIdentity converted from a pushed full-screen route to an embedded in-flow block, full-bleed art, brew button, deck row); below = the LegendBox grid + add tile. Last-active legend id persisted to `localStorage["magicdex-last-legend"]` (column later), defaults to first legend on cold start. Tapping a grid tile swaps the top block on the same surface (no push) + pins last-active. Identity block re-mounts (re-fetches deck totals) on legend change or when a brew session ends (`reloadSignal` bumped in App). Empty state: no identity block, LegendBox centers the add tile alone under "add your first legend". New `SettingsSheet.jsx` (gear glyph, top-right) holds the theme toggle + bsky colophon moved out of the old Home footer.
  - ✅ `611352d` back-ladder navigation, single root surface — back climbs one rung, Box surface is root: carousel back exits to Box (legend pinned), search/review back → carousel, deck-row review back → Box (was the now-gone LegendIdentity route). `backTarget`/`handleBack` lifted to component scope; a history back-trap (`pushState` + `popstate`) makes hardware/browser Back identical to the chevron (chevron routes through `goBack`→`history.back`); re-arms on each climb, releases on exit.
  - ✅ `0a5aae8` retire v2 routes, record v1-v2-v3 lineage in GRAVEYARD — deleted Vault.jsx, Table.jsx (Pod), Notebook.jsx (Analysis), NavBar.jsx. No working import lost (Vault was external-tool links; `ImportSheet.jsx` is orphaned/never-mounted, left in tree + flagged). GRAVEYARD.md now opens with the v1 Card Stock → v2 MTG DNA → v3 magicdex lineage + retired-tab entries.
  - ✅ `167de05` wordmark, safe-area insets, PWA manifest audit — Home header is the "magıcdex" wordmark (dotless i U+0131, Zilla Slab; "Helix" eyebrow retained, easy to drop). Safe-area insets on Home (bottom always, top in empty state), Brew back chevron, SearchScreen/ReviewScreen (dead NAV_HEIGHT→safe-area); SwipeScreen already centered in the safe area. New `public/manifest.webmanifest` (name "magicdex", standalone, portrait, dark, SVG icon) wired in index.html with theme-color + apple standalone meta. Manifest gaps for an App Store/Capacitor wrap flagged in the commit body (PNG 192/512 + maskable, real apple-touch-icon PNG, screenshots, Capacitor project, service worker).
  - ✅ Build passes (446.10 kB)
- ✅ 2026-06-12 — Brew memory + search ergonomics (`4736184`):
  - ✅ Review entered from LegendIdentity's deck row now returns to the legend on back (`reviewOrigin` state); review entered via the swipe tally still returns to swipe
  - ✅ Decided cards (any board, this session or earlier) are tracked in a live `decidedNamesRef` and excluded from every re-seed (`seedSwipeQueue`) and in-session search (`runSearch`) — skipped/browsed cards remain unaffected and can still reappear
  - ✅ SearchScreen pre-fills with the session's active query (cursor at end) via new `initialQuery` prop instead of always opening blank
  - ✅ Default swipe sort direction fixed from `desc` (Z→A) to `asc` (A→Z); existing NAME/CMC sort toggle and its ↑/↓ indicator unchanged
  - ✅ Typed searches now exclude lands by default (`-t:land` appended at search time) with a new "include lands" checkbox in the search sheet; `splitLandFilter` keeps the marker out of the visible/editable query text
  - ✅ Build passes (446.97 kB)
- ✅ 2026-06-12 — Search enforces color identity; deck view clears bottom nav (`5b02d4a`):
  - ✅ New `withColorIdentity(q, colorIdentity)` helper in Brew.jsx — appends `legal:commander ci<=<legend identity>` server-side of any query; both the auto-seed (`seedSwipeQueue`) and the in-session magnifying-glass search (`runSearch`, when `session` is set) now route through it, so illegal-colored cards can no longer surface no matter what the user types
  - ✅ `legendColorIdentity` lifted into Brew state (set once in the session-init effect) so `runSearch` has it available outside the init effect
  - ✅ ReviewScreen and LegendIdentity scroll containers get `paddingBottom: calc(60px nav height + env(safe-area-inset-bottom))` so the last maybeboard/deck row clears the fixed bottom nav (zIndex 100, above these zIndex-50 takeovers)
  - ✅ Build passes (445.89 kB)
- ✅ 2026-06-12 — Brew resume: tally reads live deck, deck row opens review (`0b6bd6f`):
  - ✅ Session start now expands existing `deck_cards` rows back into pile/decklist/maybeboard instances (`expandRows`), so the header tally and live review reflect the deck's real counts on resume instead of zero — flicks increment from there
  - ✅ LegendIdentity deck rows are tappable (44px min touch target): tapping a deck launches a session with `startView: "review"`, landing directly on that deck's live review; back goes to swipe to continue brewing (existing path)
  - ✅ `seedSwipeQueue` takes a `setView` option so a review-start session seeds its swipe queue in the background without forcing the view to swipe; the exclude set already covers every section, so the queue stays deduped against the full live deck on every entry
  - ✅ No "new deck" path from identity — one legend, one deck, by design
  - ✅ Build passes (445.67 kB)
- ✅ 2026-06-12 — Brew: flick-is-a-write persistence, save step removed (`c3fd3b6`):
  - ✅ Session start (legend-attached) now ensures the deck row exists before swiping: uses `session.deckId` if present, else inserts a new `decks` row with `legend_id = session.legend.id` (never a name-insert) and uses that
  - ✅ Each decklist/maybe flick — and its undo — is queued and applied immediately to `deck_cards` as a +/-1 quantity delta on `(deck_id, card_name, section)`, fire-and-forget with retry (3 attempts, 1.5s backoff); persistent failure surfaces a single dimmed "reconnecting…" line under the stack-count in SwipeScreen
  - ✅ Reused the existing `section` column (`decklist`/`maybe`/`pile`) as the board distinction — no new migration needed
  - ✅ Review is now a live view of the deck for legend sessions: header reads "DECK", each grouped card row gets a × remove button (writes -1), commander/build-name inputs and the SAVE BREW button are gone entirely
  - ✅ Exiting a session (swipe header back arrow) now correctly calls `handleBack` → `onSessionDone` (previously `setBrewView(backTarget)` with `backTarget === null` was a latent dead-end)
  - ✅ Non-session flows (mode-select, Loki dev seed) keep the original upsert-legend → insert-deck → bulk-insert save-on-confirm path, simplified now that the attached-deck merge branch (`saveAttachedDeck`) is gone
  - ✅ Removed the dead debounced `onSavePile` effect + `saveTimerRef` from SwipeScreen (was always a no-op since `activeDeckId` was always null)
  - ✅ Build passes (445.23 kB)
- ✅ 2026-06-12 — Brew: primary-action styling, direct-to-swipe with auto-seeded queue (`fb258d1`):
  - ✅ LegendIdentity "brew" verb restyled: full-width 48px outlined button (gold/amber border, transparent fill, gold lowercase Zilla Slab), pressed state fills gold with base-color text
  - ✅ Legend-attached brew sessions skip search/mode-select and drop straight into the swipe carousel; SwipeScreen gained a search-icon affordance (left of the tally, wired to `onGoToSearch`) to re-seed mid-session — "back" from that search returns to swipe, "back" from swipe (session) exits to LegendIdentity
  - ✅ Seed query: `legal:commander ci<=<color_identity> -t:land`, `order: edhrec`, cards already in the attached deck filtered out client-side
  - ✅ New `supabase/migrations/004_legend_color_identity.sql` (`color_identity text[]`, not yet run, see Cold Start Prompt); `color_identity` added to both lazy-backfill paths (LegendBox grid load, Brew save) and LegendBox's `legends` select
  - ✅ Build passes (444.04 kB)
- ✅ 2026-06-12 — Nav: tab icons return to root; AddLegendSheet truly above nav (`b69e9ad`):
  - ✅ App.jsx `handleNavigate(id)`: tapping Home always resets `selectedLegend` (returns to the Box from LegendIdentity); tapping Brew bumps a `brewResetSignal` that Brew watches to snap `brewView` back to "shell" without discarding pile/decklist/session state
  - ✅ Diagnosed AddLegendSheet clipping: z-index (221) was already above NavBar (100), but the sheet is rendered inside App's `overflow: hidden` content container — iOS Safari clips `position: fixed` descendants to an `overflow: hidden` ancestor's bounds even without a transform/filter (WebKit quirk), cutting off the sheet at the container edge near the nav. Fixed via `createPortal` to `document.body`.
  - ✅ Build passes (442.75 kB)
- ✅ 2026-06-11 — Legends: lazy Scryfall backfill for rows missing identity, gated art brightness lift (`7fa18ef`):
  - ✅ New `supabase/migrations/003_legend_identity.sql` — adds `type_line`, `oracle_text`, `mana_cost` to `legends` (not yet run, see Cold Start Prompt)
  - ✅ New `fetchCardIdentity(name)` in scryfall.js — exact `named?exact=` lookup, falling back to `named?fuzzy=`, returns `null` (no throw) on no match
  - ✅ LegendBox backfills any legend missing `image_uri`/`type_line` once per row (ref-tracked, no repeat on re-render), persisting `scryfall_id`/`image_uri`(art_crop)/`type_line`/`oracle_text`/`mana_cost`; rows Scryfall can't match render a flat ink-colored block + name, no error/retry
  - ✅ Brew's save path attempts the same identity lookup for newly-upserted legends (typed, not picked), best-effort/non-blocking
  - ✅ Gated (grayscale) art gets `brightness(1.45) contrast(0.95)` in dark mode (plain grayscale in light mode) — applied to LegendBox tiles and LegendIdentity hero art
  - ✅ Build passes (442.49 kB)
- ✅ 2026-06-11 — LegendIdentity: brew verb launches session attached to legend's deck (`35bc9ef`):
  - ✅ App.jsx lifts `selectedLegend` and a `brewSession {legend, deckId}` so Home ↔ Brew tab switches preserve identity-view state
  - ✅ "brew" verb sets `brewSession`, switches to the Brew tab, skips commander selection, and lands on the search screen with `sessionLabel` = legend name (label preserved across `runSearch` instead of resetting to null)
  - ✅ If the legend has an in-progress deck (<100 cards), its `deck_cards` rows are fetched on launch; on save they're merged by `card_name + section` with newly-swiped cards and the deck's rows are replaced (delete + reinsert) — updates the existing deck instead of creating a duplicate
  - ✅ Back from the search screen (or a successful save) calls `onSessionDone`, returning to Home/LegendIdentity; LegendIdentity now re-fetches its legend's `decks` on mount so counts are fresh
  - ✅ Build passes (441.24 kB)
- ✅ 2026-06-11 — Box: add legend via Scryfall search, lands grayscale at 0/100 (`2b20281`):
  - ✅ New `src/components/AddLegendSheet.jsx` — theme-token bottom sheet (not Win98-bevel CommanderSearchSheet, whose chrome matches Brew's retro look, not Home's flat aesthetic), 300ms-debounced `searchCommanders` query, borderless name/type_line result rows
  - ✅ LegendBox grid gets a final dashed-border "add legend" tile (Material Symbols `add` glyph) after all legend tiles, same tile size
  - ✅ Selecting a result upserts `{name}` into `legends` (onConflict "name", no deck created), closes the sheet, and reloads the grid via new `loadLegends()` helper — new legend lands grayscale at 0/100
  - ✅ Build passes (439.87 kB)
- ✅ 2026-06-11 — LegendIdentity: art, oracle, decks, brew verb (`df36b4a`):
  - ✅ New `src/components/LegendIdentity.jsx` — full-screen takeover (same fixed/inset/zIndex pattern as Brew), back chevron returns to Home's Box
  - ✅ Art (full-width, grayscale if gated), lowercase type_line eyebrow + Zilla Slab name + 32x1px gold/amber rule, oracle text (Noto Sans) + mana cost (Noto Sans Mono)
  - ✅ Decks list as borderless content-rows: name, "N/100" (gold "100" when complete), status, dimmed chevron
  - ✅ "brew" text verb calls `onBrew(legend, inProgressDeck)` — `inProgressDeck` = first deck with `deckTotal < 100`, else `null`; Home.jsx wires this with a `console.log` stub and routes tile taps into LegendIdentity
  - ✅ Build passes (436.43 kB)
- ✅ 2026-06-11 — Home: legend Box grid with 100-card color gate (`b3764a6`):
  - ✅ New `src/components/LegendBox.jsx` — fetches all legends with attached decks' `deck_cards(quantity)`, `deckTotal(deck) = sum(quantity) + 1` (commander not stored in deck_cards)
  - ✅ 3-column grid, 8px gaps, sharp corners; each tile shows Scryfall `art_crop` (live-fetched via `fetchCardByName` if `legends.image_uri` is empty) with name in Zilla Slab, always visible, ellipsis-truncated
  - ✅ Gate: if no attached deck has reached 100 total cards, art renders `grayscale(1)` with dimmed "N/100" (highest count) in Noto Sans Mono; at >=100, full color, no count
  - ✅ Tile tap calls `onSelectLegend(legend)`; empty state is a single dimmed mono line "no legends yet"
  - ✅ Home.jsx rewritten: removed marketing `HOME_SECTIONS`, header is now `eyebrow="Helix" title="home"`, `<LegendBox>` rendered below header
  - ✅ Build passes (432.64 kB)
- ✅ 2026-06-11 — SwipeScreen: tighter shadow, full corner mask, vertically centered card (`faa9ac5`):
  - ✅ box-shadow replaced with a tight `0 2px 8px rgba(0,0,0,0.5)` — the large soft halo (`0 8px 24px ...`) made the card read as sitting in a tray; separation now comes mostly from corners + motion
  - ✅ Card image `border-radius` increased from `4.75% / 3.5%` to `5.5% / 4%` to fully mask the white scan corners — image element only, no other element gets a radius
  - ✅ New `topReserve`/`bottomReserve`/`availableHeight` constants reserve space for the header block (~92px) and bottom gesture legend (~60px); each carousel slot now spans that band (`top`/`bottom` instead of `inset:0`) with flex centering, so the card track sits vertically centered between header and bottom nav instead of top-aligned
  - ✅ `cardHeight` now height-caps to `availableHeight` (in addition to the existing `96vw*1.4`/`440px*1.4` caps); `cardWidth` derives from `cardHeight / 1.4` (capped at `96vw`/`440px`) so the card grows to fill available vertical space at the correct aspect ratio rather than leaving margins
  - ✅ Gestures, thresholds, track logic untouched
  - ✅ Build passes (432.10 kB)
- ✅ 2026-06-11 — Brew queue: decided cards leave carousel, larger cards w/ shadow + corner mask (`f7cf828`, `81a8954`):
  - ✅ Card size bumped to `min(96vw, 440px)` wide (was `min(calc(100vw-48px),420px)`); slot spacing is now `cardWidth + 4px` so neighbors nearly touch; `getCardPx()` updated to match for browse-animation offsets
  - ✅ Each card lifted off the background with `box-shadow: 0 8px 24px rgba(0,0,0,0.55), 0 1px 4px rgba(0,0,0,0.4)` — no frame/border added
  - ✅ Card image gets a `border-radius: 4.75% / 3.5%` scan-accuracy mask (printed MTG corner ratio) — scoped to the `<img>` only, no other element gets a radius
  - ✅ `effectiveCards` now excludes any card whose `oracle_id` is in `pile`, `decklist`, or `maybeboard` (`decidedIds` memo) — decided cards leave the carousel entirely and never reappear when browsing back
  - ✅ `doMaybe`/`doDecklist` no longer increment `idx`; instead clamp `idx` to `effectiveCards.length - 2` so the track centers the next card (or new last card, if the decided card was last)
  - ✅ `doUndo` now records `pendingRestoreRef` (oracle_id); a new effect watches `effectiveCards` and jumps `idx` to the restored card's position once it reappears — restores both position and pile membership
  - ✅ "N IN STACK" count (`effectiveCards.length - idx`) now correctly reflects undecided cards only, since decided cards are excluded from `effectiveCards`
  - ✅ Build passes (432 kB)
- ✅ 2026-06-11 — True carousel track, no load fog (`f2ae4b6`):
  - ✅ Replaced the fake card-back peek slivers with a real horizontally-translating track: prev/current/next are real card images, each `min(calc(100vw - 48px), 420px)` wide, slots spaced one card-width apart so neighbors overhang the screen edges by ~24px at rest
  - ✅ `slotShift(n)` computes neighbor transforms from the same `cardWidth` calc as the current card, replacing the old fixed `92vw` spacing
  - ✅ Vertical flick still lifts only the current card (`translateY(±110vh)`); track advances afterward via the existing `idx`/offset reset — unchanged behavior, just confirmed against the new track math
  - ✅ Removed the 0.45 neighbor dimming — all slots render at full opacity except the outgoing current card during `animOut`
  - ✅ Preload trimmed from next 4 → next 3 images (`idx+1` to `idx+3`)
  - ✅ `browseNext`/`browsePrev` now offset by `getCardPx()` (`min(innerWidth - 48, 420)`) instead of `92vw`, matching the new slot spacing
  - ✅ Build passes (431 kB)
- ✅ 2026-06-11 — Peek slivers + Brew nav rework (`fc2fc46`, `5c4c6ca`):
  - ✅ Edge-peek slivers restyled as card backs: solid `#1a1208`, 1px inner border `rgba(200,150,12,0.35)` on the inward edge, widened to 20px
  - ✅ Card sizing changed to `min(calc(100vw - 56px), 420px)` (height follows at the same 1.4 aspect ratio, capped 70vh) so a 20px sliver + 8px gap fits on each side and is visible at rest, not just mid-drag
  - ✅ Header back chevron now calls a new `onExit` prop instead of `onGoToPile`; in Brew.jsx it routes to `"search"`, or to `"modes"` (the seed screen) if `sessionLabel === LOKI_SESSION_LABEL` — swipe state (index/decklist/maybeboard) is preserved either way
  - ✅ Header tally (`12 · 7`) is now a 44px-min-height button with an underline + chevron-right affordance; tapping it opens REVIEW via the existing `onGoToPile`
  - ✅ Brew.jsx's separate floating back button is now suppressed for `brewView === "swipe"` (SwipeScreen's own header chevron replaces it); still rendered for modes/search/review
  - ✅ Review → swipe still resumes at `swipeIndex` via existing `backTarget`/`initialIndex` plumbing — unchanged
  - ✅ Build passes (432 kB)
- ✅ 2026-06-11 — SwipeScreen card sizing, edge peek, persistent header (`ee0d310`, `26089df`):
  - ✅ Centered card now dominates: `min(92vw, 420px)` wide, height `min(calc(92vw*1.4), calc(420px*1.4), 70vh)`, replacing the old 88vw/62vh sizing
  - ✅ Edge-peek slivers (16px, `var(--color-surface)`) at left/right screen edges when a prev/next card exists; track the horizontal drag offset with `stripTransition`
  - ✅ Gesture thresholds (axis lock, browse/flick ratios + velocities, spring-back) untouched
  - ✅ New persistent header bar (transparent, zIndex 3): back chevron → `onGoToPile` (exits to review, keeps decisions), center legend name in Zilla Slab, right running tally `decklist.length · maybeboard.length` in Noto Sans Mono
  - ✅ Existing stack-info strip (count/UNDO/SORT) shifted down 40px to make room; sort dropdown offset adjusted to match
  - ✅ Review → swipe back-navigation already worked via existing `backTarget` logic in Brew.jsx (review → swipe) and persisted state — no Brew.jsx changes needed
  - ✅ Build passes (431 kB)
- ✅ 2026-06-11 — Loki dev seed (`24b531b`):
  - ✅ `LOKI_CLONE_QUERY` exported from `src/lib/scryfall.js` (`legal:commander ci<=u t:creature o:"copy of" -o:"token"`) — hardcoded starting point, flagged do-not-improve; live-checked: 51 cards
  - ✅ "// Loki test session" dev row on the mode-select screen (Noto Sans Mono, dimmed) — skips SearchScreen straight to SwipeScreen via the existing `runSearch` path; shows loading/error inline
  - ✅ Session label "Loki, God of Mischief — oops all clones" shown in SwipeScreen's top strip via the `commanderCard` name prop; `sessionLabel` state cleared on normal searches and `resetBrew`
  - ✅ Save/review flow untouched; build passes (430 kB)
- ✅ 2026-06-11 — SwipeScreen carousel gesture model (`ed8ea5c`):
  - ✅ Horizontal swipe is browsing only — Instagram-style carousel (left = next, right = previous), animated slide, prev/next cards peek in from the edges at 0.45 opacity (92vw slot spacing)
  - ✅ Flick up (velocity > 0.6 px/ms or drag past 30% viewport height) → mainboard; flick down → maybeboard; card flies off, carousel advances
  - ✅ Axis lock at 10px: horizontal can never become a flick, vertical can never become a browse; under-threshold vertical release springs back
  - ✅ Left/right no longer sorts: `doResolve` (keep→pile / pass) removed; pile props, undo `kept` branch, and autosave plumbing left intact but unreachable by gesture
  - ✅ Keyboard remapped: ArrowRight/Left browse, ArrowUp/Down still decide; gesture legend now "← browse →  ↑ mainboard  ↓ maybe"
  - ✅ Build passes (429 kB)
- ✅ 2026-06-10 — Brew overhaul (visual/interaction pass):
  - ✅ Forced dark palette for all Brew sub-screens (`BREW` constants in Brew.jsx: #0a0e1a base, #e8a020 amber, #7ab89a green) — sub-screens no longer follow app light mode
  - ✅ Mode-select screen between chip tap and search: New Legend / Import Deck / Free Pile / Card Discovery, PageHeader pattern (HELIX eyebrow, brew title, amber rule), ToolChips-style rows
  - ✅ Card display frameless — removed surface background, bevel border, box shadow, and the full-screen gradient overlay
  - ✅ Persistent gesture legend (`← pass  ↑ mainboard  ↓ maybe  → keep`, Noto Sans Mono, dimmed) replaces the one-time animated hint; tip code + TIP_KEY usage removed
  - ✅ Swipe rewritten flat: translate-only (no rotation), no KEEP/PASS/DECKLIST/MAYBE stamps, no color tint; up flies to top (mainboard), down to bottom (maybe)
  - ✅ SearchScreen SEARCH button white Win98 bevels removed; done-state buttons re-palette'd; scope held to Brew files only

## Done (earlier)
- ✅ 2026-06-10 — Legends schema + full brew flow (prompts 1–5):
  - ✅ P1: `supabase/migrations/002_legends.sql` written — legends table, `decks.legend_id` + `build_name`, `url`/`platform` made nullable. **NOT yet run** (needs SQL editor; no CLI/service key available)
  - ✅ P2: Vault deck registry restored from `a0a6016` (it was deliberately stripped in the e968083 redesign) with the two-field pattern: commander name → legends upsert, build name → `decks.build_name`; legacy `legend` text column kept in sync; `deck_name` insert dropped (column doesn't exist in live schema)
  - ✅ P3: `onSearch` wired to `fetchFirstPageForSwipe` (≤175 cards/page); errors render in SearchScreen; sort changes re-fetch
  - ✅ P4: four-direction gesture map in SwipeScreen — left pass, right keep, up decklist (new board + prop), down maybe; dominant axis wins; DECKLIST/MAYBE drag labels; ArrowUp/Down keys; undo covers all three boards; useGameChangers + useDoubleTap (flip) now used
  - ✅ P5: new ReviewScreen (presentational) groups pile/decklist/maybe with quantities; save flow in Brew.jsx: upsert legend → insert deck → batch-insert deck_cards (`section` ∈ pile/decklist/maybe)
  - ✅ `vite build` passes (438 kB — swipe/review now bundled); live schema verified via REST probes (anon writes pass RLS)
- ✅ 2026-06-10 — SearchScreen copy cleanup:
  - ✅ Removed "DECK STACK" heading, "Search. Swipe. Brew." tagline, Deck Stack description line, swipe-hint line, and the Bluesky/GitHub footer links
  - ✅ Copy removal only — no layout/logic/style edits; spacer + input + progress bar + SEARCH button untouched
  - ✅ `vite build` passes; grep confirms no target copy remains
- ✅ 2026-06-10 — Helix: Brew tap target wired:
  - ✅ Brew.jsx: `brewView` state ("shell" | "search"); shell renders PageHeader + ToolChips as before; "search" renders SearchScreen in a position:fixed inset-0 zIndex:50 takeover with `brewThemeVars()` re-added, scoped to the takeover wrapper
  - ✅ tools.js: Helix: Brew entry carries `action: "brew-search"`; Brew.jsx resolves it to a live onClick (static data can't close over component state)
  - ✅ ToolChips.jsx: rows call `tool.onClick` on tap; `target="_blank"`/`rel` only set when a `url` exists; pointer cursor on actionable rows
  - ✅ Back button rendered in the takeover wrapper (top-left, 44px target) — exits to shell
  - ✅ `vite build` passes (210 kB; SearchScreen + settings back in bundle)
- ✅ 2026-06-10 — Brew tab reverted + tools data:
  - ✅ `src/pages/Brew.jsx` restored byte-identical to pre-prompt-7 shell (PageHeader + ToolChips)
  - ✅ "Helix: Brew" added as first BREW_TOOLS entry, no tier, same shape as other entries
  - ✅ ToolChips renders untiered entries above tier groups (see Known Issues — required deviation)
  - ✅ `vite build` passes; bundle back to 203 kB (brew-components tree-shaken once unreferenced)
- ✅ 2026-06-10 — Brew port, prompts 1–8:
  - ✅ P1–4: `scryfall.js`, `wrec.js` → `src/lib/`; `useDoubleTap.js`, `useGameChangers.js` → `src/hooks/`; `brewPrompt.js`, `validateBrewQuery.js` → `src/services/` (byte-exact copies)
  - ✅ P5: 9 components → `src/brew-components/`, 3 screens → `src/brew-components/screens/` (byte-exact copies)
  - ✅ P6: font swaps (Space Grotesk/DM Sans → Noto Sans, IBM Plex Mono → Noto Sans Mono), import-path fixes for the screens subfolder, `NAV_HEIGHT` inlined (60), Deck Stack CSS vars bridged to MTG DNA theme tokens via `brewThemeVars()` in `src/pages/Brew.jsx`
  - ✅ P7: Brew tab renders SearchScreen below PageHeader; ToolChips removed from Brew (still used by Notebook/Table/Vault); supabase client passed as prop
  - ✅ P8: localStorage keys renamed (`ds_search_history`→`helixbrew_search_history`, `ds_swipe_hint_shown`→`helixbrew_swipe_hint_shown`, `cardstock_settings`→`helixbrew_settings`); zero deck-stack auth/db/supabase imports; `vite build` passes; all 12 brew files parse clean

## Done (housekeeping)
- ✅ 2026-06-10 — GRAVEYARD.md added at repo root: prototype consolidation record (deck-stack donor, pod-check / life-track / gold-fish / after-school-special sealed)

## Known Issues
- **Closed bottom sheets aren't inert (2026-07-10)**: AddLegendSheet/SettingsSheet slide off-screen when closed but stay keyboard-focusable and in the accessibility tree (Tab reaches their controls; screen readers would announce them). Mobile touch users are unaffected. Fix candidate: `inert`/`aria-hidden` on the closed portals; the same pass could make LegendIdentity's deck row a real `<button>` (it's a div with onClick — no keyboard/AT access).
- **Empty deck list has no first-run copy (2026-07-10)**: a brand-new user's first deck-list view is "DECKLIST · 0" + "—" with BREW emphasized in the nav — functional, but nothing says what brew does. Design-tier copy call (one line, e.g. "brew deals cards matched to your legend"); fold into the visual-language session with Ben, not autonomous.
- **16px favicon accent-dots muddy (2026-07-04)**: the three graduated accent dots merge into a single gold smudge at 16×16 (fine at 32+). Recognizability is unaffected (dark tile + gold lens is the anchor). If it ever bothers, generate a simplified 16px variant in `gen-icons.mjs` that drops the dots/panel-line/play-triangle. Regenerate any icon with `npm run gen:icons`.
- **Legacy Brew modes orphaned (2026-06-12)**: with the tab bar gone, Brew is only ever launched with a legend session. The "shell" tool-landing and the BREW_MODES select (New Legend / Import Deck / Free Pile / Card Discovery) are now unreachable. The code is retained (Brew renders a dark loading guard instead of the shell when a session is present). `data/tools.js` still exports the now-unused TOOLS/ANALYZE_TOOLS/PLAY_LIFE_TOOLS/PLAY_POD_TOOLS lists.
- **Orphaned ImportSheet (2026-06-12)**: `brew-components/ImportSheet.jsx` is defined but never mounted — kept intentionally as the donor for a future real Moxfield/Archidekt import wired into the add-legend flow (flagged in GRAVEYARD). No working import path exists yet.
- **History back-trap is dev-fragile under StrictMode (2026-06-12)**: the brew session's `popstate` trap pushes one synthetic history entry on overlay open; React StrictMode's dev double-mount can push a second (prod mounts once, correct). Hardware-Back parity can only be verified on a real device/build, not the desktop dev preview.
- **Embedded identity refresh relies on remount (2026-06-12)**: the top identity block re-fetches decks only on `key` change (`legendId-reloadSignal`); App bumps `reloadSignal` when a brew session ends, so deck totals refresh on return. A save that happens without ending the session (none currently) would not refresh until the key changes.
- **Commit boundary slip (2026-06-11)**: the header nav changes (`onExit` prop, tally button) landed in `fc2fc46` ("peek slivers") rather than `5c4c6ca` ("Brew nav") since both edits hit SwipeScreen.jsx before the first commit. No functional issue — just noting the split doesn't match the two commit messages exactly.
- **Pile is now gesture-unreachable (2026-06-11; partly resolved 2026-06-12)**: with horizontal repurposed to browsing, nothing routes cards to the pile anymore. The ReviewScreen pile section was deleted (`5f3991c`). Still lingering: `pile` state in Brew, the `pile` section in `commitCard`/`handleRemoveCard`/`buildCardRows`, SwipeScreen's done-state "N pile" line and `decidedIds` pile membership, and `expandRows(..., "pile")` on resume. Decide whether to excise the pile concept entirely or give it a new affordance.
- **"X KEPT" done-state label**: top strip still reports `pile.length` as KEPT when the stack is exhausted — stale wording now that keep/pass is gone.
- **Vault reverted to shell (2026-06-10)**: per request, Vault.jsx is back to the bare PageHeader + ToolChips state (`b63d5cd^` / e968083, byte-identical). The commander+build registry form from `b63d5cd` is gone again — recover it from that commit if wanted. `lib/fetchDecklist.js` is now unused by any page.
- **Migration 002 not run**: Brew saves will fail with column/table errors until `002_legends.sql` runs in the SQL editor. The app builds and browses fine without it.
- **Brew board naming**: internal state/props/db section say `decklist`; user-facing copy now says "mainboard" (gesture legend, done state). Rename pass may be wanted before the section values calcify in deck_cards rows.
- **First-run swipe tip removed**: `helixbrew_swipe_hint_shown` is no longer read or written (persistent gesture legend replaced it); stale keys may linger in users' localStorage harmlessly.
- **decks.platform CHECK constraint**: live values allow at least `moxfield`/`archidekt`; brew-created decks insert `platform: null` (allowed once 002 makes it nullable). If a 'brew' platform value is wanted later, the CHECK needs altering.
- **Write-tool NUL corruption (caught)**: two NUL bytes landed in Brew.jsx during this session's write (would have corrupted saved card rows); stripped and verified repo-wide — no other text files affected.
- **Commander identity is name-only**: legends rows store just `name` for now; `scryfall_id`/`image_uri` are nullable and unpopulated. Enrichment via `fetchCardByName` is a natural follow-up.
- **SwipeScreen sort re-fetches mid-stack**: changing sort re-runs the search and resets the index; already-swiped cards reappear (dedup only filters pile, not decklist/maybe).
- **SearchScreen has no heading at all now**: the Deck Stack copy was removed without replacement (per spec), so the takeover shows a mostly-empty screen with the input/button pushed to the bottom by the flex spacer. Needs Helix: Brew copy in the design pass.
- **SearchScreen ignores `onBack`**: it has no back affordance or `onBack` prop in its JSX. Brew.jsx passes `onBack` anyway (future use) and renders its own back button in the takeover wrapper as the actual exit.
- **`onSearch` is a stub**: console.log only. SwipeScreen/PileScreen pile flow not wired; those components plus sheets/modals/services remain unreferenced and tree-shaken.
- **"No other files touched" deviation (2026-06-10)**: ToolChips groups strictly by tier S/A/B — a tierless entry would not render at all. To make "Helix: Brew" appear first with no tier label, `src/components/ToolChips.jsx` was modified: row markup extracted to a shared `renderRow`, untiered entries render above the tier groups with identical styling and no heading. Same file later gained `tool.onClick` support for the tap wiring.
- **Source snapshot**: deck-stack HEAD deleted SwipeScreen/SearchScreen/PileScreen in commit `33f167f` ("demolish old swipe/pile/search architecture", 2026-05-20). All files were copied from `33f167f^`, the last commit where screens and components coexisted. This predates deck-stack's May 21–23 redesign commits (Noto Sans migration, Y2K-strip) — fonts were handled in P6 anyway, but the copies are NOT deck-stack HEAD.
- **P6 deviation**: the prompt's find-and-replace spec (Bebas Neue/DM Sans literals, `--bg`/`--panel` var set, auth/db import strips) didn't match the snapshot — files had no auth/db imports, fonts were mostly `var(--font-system)`, and styling uses deck-stack's Win98 token system (`--color-*`, `--bevel-*`, `--space-*`) in module-level style objects that can't call useTheme(). Instead of ~300 inline rewrites, all deck-stack CSS vars are defined from MTG DNA theme tokens in `brewThemeVars()` (Brew.jsx). Components keep `var(--x)` references and re-theme automatically, including light/dark.
- **P7 deviation**: SearchScreen's props are `{ onSearch, loading, error, commanderCard, onCommanderCardChange }` — it doesn't accept a Supabase client. The client is passed as a `supabase` prop anyway (ignored for now). `onSearch` is a console.log stub; the search→pile flow isn't wired.
- **Extra deps copied** (not in the prompt list, required by imports): `src/lib/settings.js`, `src/constants/wrec.js`. `NAV_HEIGHT` (60) inlined where BottomNav was imported — verify against MTG DNA's actual NavBar height in the visual pass.
- SearchScreen still shows "DECK STACK" branding, deck-stack's Bluesky/GitHub footer links, and `error` prop is never rendered in its JSX.
- `services/brewPrompt.js` + `validateBrewQuery.js` and most brew components (SwipeScreen, PileScreen, sheets, modals) are copied but not yet reachable from the app — Vite tree-shakes them until wired.
